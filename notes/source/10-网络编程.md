<a name="OPss7"></a>
## 10.网络编程
Berkeley Socket<br />库已经完成了传输层及其下各层的基本功能，即本库工作在应用层。<br />地址：IP地址 ： 端口号<br />$ man 7 socket
```c
//man 7 ip
struct sockaddr_in {
    sa_family_t sin_family; /* address family: AF_INET */ //地址类型 ipv4
    in_port_t sin_port; /* port in network byte order */
    struct in_addr sin_addr; /* internet address */
};
/* Internet address. */
struct in_addr {
    uint32_t s_addr; /* address in network byte order */
};
```

网络字节序（network byte order）<br />![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650594287168-c932dcdc-eb28-481d-a062-a4aeca1c961b.png#clientId=uc055a1f1-cc9f-4&crop=0&crop=0&crop=1&crop=1&from=paste&height=318&id=u46467243&margin=%5Bobject%20Object%5D&name=image.png&originHeight=531&originWidth=902&originalType=binary&ratio=1&rotation=0&showTitle=false&size=236962&status=done&style=none&taskId=ue43a00b2-38d8-4858-832c-07450011d13&title=&width=540.727294921875)<br />大小端转换
```c
#include <stdio.h>
/*联合*/
union node
{
    int num;
    char ch;
}
 
int main()
{
    union node p;
    //方法一
    p.num = 0x12345678;
    if (p.ch == 0x78)
    {
        printf("Little endian\n");
    }
    else
    {
        printf("Big endian\n");
    }
   
    //方法二
    int num = 0x12345678;
    char *q = （char *）&num
    if (*q == 0x78)
    {
        printf("Little endian\n");
    }
    else
    {
        printf("Big endian\n");
    }
    return 0;
}
```

 下面是整数大小端转换相关的函数。  
```c
#include <arpa/inet.h>
//h --> host n --> net l --> 32bit s --> 16bit
uint32_t htonl(uint32_t hostlong);
uint16_t htons(uint16_t hostshort);
uint32_t ntohl(uint32_t netlong);
uint16_t ntohs(uint16_t netshort)
```
```c
#include <43func.h>
int main()
{
    unsigned short s = 0x1234;
    printf("s = %x\n", s);
    printf("htons(s) = %x\n", htons(s));
    printf("ntohs(s) = %x\n", ntohs(s));
    unsigned int i = 0x12345678;
    printf("i = %x\n", i);
    printf("htonl(i) = %x\n", htonl(i));
    printf("ntohl(i) = %x\n", ntohl(i));
}

/*
s = 1234
htons(s) = 3412
ntohs(s) = 3412
i = 12345678
htonl(i) = 78563412
ntohl(i) = 78563412
*/
```

- `printf`       用小端
-   sockaddr      用大端

IP地址的大小端转换
```c
#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>
int inet_aton(const char *cp, struct in_addr *inp);// 点分十进制 -> 32位大端整数
in_addr_t inet_addr(const char *cp);// 点分十进制 -> 32位大端整数
char *inet_ntoa(struct in_addr in); // 32位大端整数 -> 字符串
in_addr_t inet_addr(const char *cp);
```
```c
#include <43func.h>
int main(int argc, char *argv[]){
    // ./ip 192.168.14.9
    ARGS_CHECK(argc,2);
    struct in_addr addr;
    inet_aton(argv[1],&addr);
    printf("addr = %08x\n",addr.s_addr);
}
```
printf : （小端）

<a name="oUHRn"></a>
#### 书写IP地址
```c
#include <43func.h>
int main(int argc, char *argv[]){
    // ./addr 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;//表示所使用是IPV4
    addr.sin_port =  htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    printf("port = %x\n", addr.sin_port);
    printf("ip = %08x\n", addr.sin_addr.s_addr);
}
```

<a name="PC4Xq"></a>
#### 域名 -> IP

1. 先查本地host

$ cat /proc/hosts

2. 查DNS （电信 202.103.24.68 ）

用代码 获取域名对应的IP地址<br />#include <netdb.h><br />struct hostent *gethostbyname(const char *name);<br />// 联网查询DNS域名服务器
```c
struct hostent {
    char *h_name; 	    /* official name of host */
    char **h_aliases;   /* alias list */
    int h_addrtype;     /* host address type */
    int h_length;       /* length of address */
    char **h_addr_list; /* list of addresses */
}
```

获取远程主机的信息<br />`gethostbyname` - get network host entry<br />#include <netdb.h><br />struct hostent *gethostbyname(const char *name);
```c
#include <43func.h>
int main(int argc, char *argv[]){
    // ./gethostbyname www.baidu.com
    ARGS_CHECK(argc,2);
    struct hostent *pHost = gethostbyname(argv[1]);
    if(pHost == NULL){
        fprintf(stderr,"gethostbyname:%s\n", strerror(h_errno));
        return -1;
    }
    printf("Host real name = %s\n",pHost->h_name);
    for(int i = 0;pHost->h_aliases[i] != NULL; ++i){
        printf("    alias name = %s\n", pHost->h_aliases[i]);
    }
    printf("addrtype = %d\n", pHost->h_addrtype);
    printf("addrLength = %d\n", pHost->h_length);
    for(int i = 0;pHost->h_addr_list[i] != NULL; ++i){
        char buf[1024] = {0};
        inet_ntop(pHost->h_addrtype,pHost->h_addr_list[i],buf,sizeof(buf));
        printf("   addr = %s\n", buf);
    }
}
```


<a name="aF9Yg"></a>
#### 基于TCP的网络通信
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650607487072-80d0af01-06db-4142-ade1-b910577f5683.png#clientId=uc055a1f1-cc9f-4&crop=0&crop=0.0075&crop=1&crop=1&from=paste&height=400&id=u3ca665a1&margin=%5Bobject%20Object%5D&name=image.png&originHeight=551&originWidth=490&originalType=binary&ratio=1&rotation=0&showTitle=false&size=24521&status=done&style=none&taskId=ub5278dda-5bb7-4bec-9238-afddfa225f9&title=&width=356)<br />网络的用法和文件一样。

<a name="C0YCo"></a>
### `socket`
$ man 2 socket<br />![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650637305018-921ccf87-b5c1-4df2-bec5-d2ddb8502a18.png#clientId=uc055a1f1-cc9f-4&crop=0.0276&crop=0.0222&crop=0.9842&crop=0.9778&from=paste&height=130&id=u10a573b2&margin=%5Bobject%20Object%5D&name=image.png&originHeight=186&originWidth=349&originalType=binary&ratio=1&rotation=0&showTitle=false&size=12926&status=done&style=none&taskId=u809d8d1f-1140-46b8-8964-9c31db0d964&title=&width=243)<br />`socket` - create an endpoint for communication<br />#include <sys/types.h>          /* See NOTES */<br />#include <sys/socket.h><br />int socket(int domain, int type, int protocol);

- int domain
   - AF_UNIX      Local communication                unix(7)
   - AF_LOCAL     Synonym for AF_UNIX
   - AF_INET      IPv4 Internet protocols               ip(7)
- int type
   - SOCK_STREAM     Provides  sequenced,  reliable,  two-way,  connection-based byte streams.  An out-of-band data

                       transmission mechanism may be supported.

   - SOCK_DGRAM      Supports datagrams (connectionless, unreliable messages of a fixed maximum length).
- int protocol  
   - 0  // 表示自动获取协议

```c
#include <43func.h>
int main(){
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    //创建一个socket 支持IPv4和TCP
    ERROR_CHECK(sockFd,-1,"socket");
    printf("sockFd = %d\n", sockFd);
    close(sockFd);
}
```

$ man 2 connect<br />`connect` - initiate a connection on a socket <br />// **connect 和 listen 配合实现三次握手**<br />#include <sys/types.h>          /* See NOTES */<br />#include <sys/socket.h><br />int connect(int sockfd, const struct sockaddr *addr,<br />                   socklen_t addrlen);

- int sockfd
   - ret of socket
- const struct sockaddr *addr
   - 原始不好用版本
   - 兼容好用版本  -  struct sockaddr_in
   - 构造地址用 sochetadd_in 
   - 调用函数先取地址，再对指针进行( struct sochaddr_in * )强制转换
- socklen_t addrlen
```c
#include <43func.h>
int main(int argc,char *argv[]){
    // ./connect 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    //创建一个socket 支持IPv4和TCP
    ERROR_CHECK(sockFd,-1,"socket");
    printf("sockFd = %d\n", sockFd);
    struct sockaddr_in addr;//创建时 用 sockaddr_in
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    //调用函数时，先取地址再强转
    int ret = connect(sockFd,(struct sockaddr *)&addr,sizeof(addr));
  
    close(sockFd);
}
```
先socket 指定服务器地址，访问connect，发第一次握手

Linux 下抓包 tcpdump<br />先 socket 指定服务端地址， 调用 connect  第一次握手

TCP连接的标志<br />如何区分两条不同的TCP连接  <br />**四元组      < 源IP，源端口，目的IP，目的端口 >**<br />服务端的IP和端口一般是固定的<br />一个TCP连接最多可以创建65535 个端口？(  **X  **)

`bind` - bind a name to a socket<br />#include <sys/types.h>          /* See NOTES */<br />#include <sys/socket.h><br />int bind(int sockfd, const struct sockaddr *addr, socklen_t addrlen);<br />绑定一个IP给socket 一般是服务端使用
```c
#include <43func.h>
int main(int argc,char *argv[]){
    // ./connect 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    //创建一个socket 支持IPv4和TCP
    ERROR_CHECK(sockFd,-1,"socket");
    printf("sockFd = %d\n", sockFd);
    struct sockaddr_in addr;//创建时 用 sockaddr_in
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    // 调用函数时，先取地址再强转
    // 对于bind而言， addr一定是本地地址
    int ret = bind(sockFd, (struct sockaddr *)&addr, sizeof(addr);
    ERROR_CHECK(ret,-1,"bind");
    close(sockFd);
}
```


`listen` -  listen for connections on a socket<br />#include <sys/types.h>          /* See NOTES */<br />#include <sys/socket.h><br />int listen(int sockfd, int backlog);

- int sockfd
- int backlog  半连接队列的长度

![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650638594629-7ff18b93-465c-4416-b2b2-aed9814ccc54.png#clientId=uc055a1f1-cc9f-4&crop=0&crop=0&crop=1&crop=1&from=paste&height=128&id=u9a0e27cd&margin=%5Bobject%20Object%5D&name=image.png&originHeight=176&originWidth=654&originalType=binary&ratio=1&rotation=0&showTitle=false&size=19064&status=done&style=none&taskId=u76cd6656-6899-4ec9-859c-7b5272a586b&title=&width=475.6363636363636)<br />`listen`是说明让socket当作服务端来使用<br />`listen`是清空socket的文件对象，建立了半连接队列和全连接队列<br />65535  65536  mss
```c
#include <43func.h>
int main(int argc,char *argv[]){
    // ./server 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    //创建一个socket 支持IPv4和TCP
    ERROR_CHECK(sockFd,-1,"socket");
    printf("sockFd = %d\n", sockFd);
    struct sockaddr_in addr;//创建时 用 sockaddr_in
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    //调用函数时，先取地址再强转
    //对于bind而言 addr一定是本地地址
    int ret = bind(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"bind");
    ret = listen(sockFd,10);
    ERROR_CHECK(ret,-1,"listen");
    // sleep(20);
    close(sockFd);
}
```


`accept` - accept a connection on a socket<br />#include <sys/types.h>          /* See NOTES */<br />#include <sys/socket.h><br />int accept(int sockfd, struct sockaddr *addr, socklen_t *addrlen);

- int sockfd
   - listen 的 fd
- struct sockaddr *addr
   - addr填本地地址 ，获取的对端地址会替换值
   - 填NULL 不获取对端地址
- socklen_t *addrlen
   - 先申请一个socklen_t，再取地址传递（不能填０，填addr的长度）

后两个全填NULL，不获取客户端信息<br />_value_result  传入传出参数_<br />![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650638782183-00c83d7c-6b81-4f17-bc80-d3b6a47c1613.png#clientId=uc055a1f1-cc9f-4&crop=0.0173&crop=0&crop=0.9704&crop=1&from=paste&height=176&id=uaaac284a&margin=%5Bobject%20Object%5D&name=image.png&originHeight=254&originWidth=557&originalType=binary&ratio=1&rotation=0&showTitle=false&size=39138&status=done&style=none&taskId=u13cf4def-5a71-499d-95ab-8d9484fda35&title=&width=386)<br />accept从**listen socket**的全连接队列中取出一个连接（**sockFd  建立连接**），构建一个新的已连接socket （**netFd 发送数据**）<br />accept的本质是一个read，若连接队列为空，accept就会阻塞
```c
#include <43func.h>
int main(int argc,char *argv[]){
    // ./server 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    //创建一个socket 支持IPv4和TCP
    ERROR_CHECK(sockFd,-1,"socket");
    printf("sockFd = %d\n", sockFd);
    struct sockaddr_in addr;//创建时 用 sockaddr_in
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    //调用函数时，先取地址再强转
    //对于bind而言 addr一定是本地地址
    int ret = bind(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"bind");
    ret = listen(sockFd,10);
    ERROR_CHECK(ret,-1,"listen");
    
    struct sockaddr_in clientAddr;
    //socklen_t clientAddrLen = sizeof(clientAddr);//写0是会出问题
    socklen_t clientAddrLen = sizeof(clientAddr);//写0是会出问题
    int netFd = accept(sockFd,(struct sockaddr *)&clientAddr,&clientAddrLen);
    ERROR_CHECK(netFd,-1,"accept");
    printf("netFd = %d\n", netFd);
    printf("client addr length = %d\n", clientAddrLen);
    printf("client family = %d\n", clientAddr.sin_family);
    printf("client port = %d\n", ntohs(clientAddr.sin_port));
    printf("client ip = %s\n",inet_ntoa(clientAddr.sin_addr));
    // int netFd = accept(sockFd,NULL,NULL);
    // ERROR_CHECK(netFd,-1,"accept");
    // printf("netFd = %d\n", netFd);
    
    close(sockFd);
}
```

<a name="aHycx"></a>
### 读写网络缓冲区
`read` `write`可以用于** 客户端socket**和**服务端的已连接socket**（不能用于服务端的listen socket）

`send` - send a message on a socket<br />#include <sys/types.h><br />#include <sys/socket.h><br />ssize_t send(int sockfd, const void *buf, size_t len, int flags);

- int flags   // 在对socket操作时,填0时 和write等价

**只能操作socket**<br />send和write是**拷贝数据到SND缓冲区**，后续操作由**内核协议栈**进行。

`recv` - receive a message from a socket<br />#include <sys/types.h><br />#include <sys/socket.h><br />ssize_t recv(int sockfd, void *buf, size_t len, int flags);

- int flags   // 在对socket操作时,填0时 和read等价

**只能操作socket**<br />recv和read是**拷贝数据从RECV缓冲区到用户空间**，后续操作由**内核协议栈**进行。

send  recv
```c
#include <43func.h>
int main(int argc,char *argv[]){
    // ./connect 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    //创建一个socket 支持IPv4和TCP
    ERROR_CHECK(sockFd,-1,"socket");
    printf("sockFd = %d\n", sockFd);
    struct sockaddr_in addr;//创建时 用 sockaddr_in
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    //调用函数时，先取地址再强转
    int ret = connect(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"connect");
    char buf[4096] = {0};
    ret = recv(sockFd,buf,sizeof(buf),0);
    puts(buf);
    // ret = send(sockFd,"nihao from client",17,0);
    close(sockFd);
}
```
```c
#include <43func.h>
int main(int argc,char *argv[]){
    // ./server 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    //创建一个socket 支持IPv4和TCP
    ERROR_CHECK(sockFd,-1,"socket");
    printf("sockFd = %d\n", sockFd);
    struct sockaddr_in addr;//创建时 用 sockaddr_in
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    //调用函数时，先取地址再强转
    //对于bind而言 addr一定是本地地址
    int ret = bind(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"bind");
    ret = listen(sockFd,10);
    ERROR_CHECK(ret,-1,"listen");
    struct sockaddr_in clientAddr;
    //socklen_t clientAddrLen = /*sizeof(clientAddr)*/ 0;//写0是会出问题
    socklen_t clientAddrLen = sizeof(clientAddr);//写0是会出问题
    int netFd = accept(sockFd,(struct sockaddr *)&clientAddr,&clientAddrLen);
    ERROR_CHECK(netFd,-1,"accept");
    printf("netFd = %d\n", netFd);
    printf("client addr length = %d\n", clientAddrLen);
    printf("client family = %d\n", clientAddr.sin_family);
    printf("client port = %d\n", ntohs(clientAddr.sin_port));
    printf("client ip = %s\n",inet_ntoa(clientAddr.sin_addr));
    // int netFd = accept(sockFd,NULL,NULL);
    // ERROR_CHECK(netFd,-1,"accept");
    // printf("netFd = %d\n", netFd);
    ret = send(netFd,"hello from server",17, 0);
    ERROR_CHECK(ret,-1,"send");
    char buf[4096] = {0};
    ret = recv(netFd,buf,sizeof(buf),0);
    ERROR_CHECK(ret,-1,"recv");
    puts(buf);
    close(netFd);
    close(sockFd);
}
```

send两次，recv一次可以收到全部消息，一条TCP消息
```c
// send_twice.c
#include <43func.h>
int main(int argc,char *argv[]){
    // ./server 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    //创建一个socket 支持IPv4和TCP
    ERROR_CHECK(sockFd,-1,"socket");
    printf("sockFd = %d\n", sockFd);
    struct sockaddr_in addr;//创建时 用 sockaddr_in
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    //调用函数时，先取地址再强转
    //对于bind而言 addr一定是本地地址
    int ret = bind(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"bind");
    ret = listen(sockFd,10);
    ERROR_CHECK(ret,-1,"listen");
    struct sockaddr_in clientAddr;
    //socklen_t clientAddrLen = /*sizeof(clientAddr)*/ 0;//写0是会出问题
    socklen_t clientAddrLen = sizeof(clientAddr);//写0是会出问题
    int netFd = accept(sockFd,(struct sockaddr *)&clientAddr,&clientAddrLen);
    ERROR_CHECK(netFd,-1,"accept");
    printf("netFd = %d\n", netFd);
    printf("client addr length = %d\n", clientAddrLen);
    printf("client family = %d\n", clientAddr.sin_family);
    printf("client port = %d\n", ntohs(clientAddr.sin_port));
    printf("client ip = %s\n",inet_ntoa(clientAddr.sin_addr));
    // int netFd = accept(sockFd,NULL,NULL);
    // ERROR_CHECK(netFd,-1,"accept");
    // printf("netFd = %d\n", netFd);
    send(netFd,"nihao",5,0);
    send(netFd,"huaidan",7,0);
    close(netFd);
    close(sockFd);
}

// connet.c
#include <43func.h>
int main(int argc,char *argv[]){
    // ./connect 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    //创建一个socket 支持IPv4和TCP
    ERROR_CHECK(sockFd,-1,"socket");
    printf("sockFd = %d\n", sockFd);
    struct sockaddr_in addr;//创建时 用 sockaddr_in
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    //调用函数时，先取地址再强转
    int ret = connect(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"connect");
    char buf[4096] = {0};
    ret = recv(sockFd,buf,sizeof(buf),0);
    puts(buf);
    // ret = send(sockFd,"nihao from client",17,0);
    close(sockFd);
}

```
send放入缓冲区，实际发送由内核协议栈发送<br />TCP是一种字节流协议，消息与消息之间无边界<br />MSS对用户层无影响，由内核控制发送 MSS ( 1460 )

如何使接收方和发送方解读相同的信息 - 用户层自定义协议

<a name="xCs7j"></a>
### 利用socket实现即时聊天
基本上和管道一样，socket的R，W的fd是同一个<br />client : socket  ->  connect<br />server :socket  -> bind  ->  listen  ->  accept

```c
#include <43func.h>
int main(int argc, char *argv[]){
    // ./client 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    int ret = connect(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"connect");

    fd_set rdset;
    char buf[4096] = {0};
    while(1){
        FD_ZERO(&rdset);
        FD_SET(STDIN_FILENO,&rdset);
        FD_SET(sockFd,&rdset);
        select(sockFd+1,&rdset,NULL,NULL,NULL);
        if(FD_ISSET(STDIN_FILENO,&rdset)){
            bzero(buf,sizeof(buf));
            ret = read(STDIN_FILENO,buf,sizeof(buf));
            if(ret == 0){
                send(sockFd,"nishigehaoren",13,0);
                break;
            }
            send(sockFd,buf,strlen(buf),0);
        }
        if(FD_ISSET(sockFd,&rdset)){
            bzero(buf,sizeof(buf));
            ret = recv(sockFd,buf,sizeof(buf),0);
            if(ret == 0){
                puts("chat is end!");
                break;
            }
            puts(buf);
        }
    }
    close(sockFd);
}
```
```c
#include <43func.h>
int main(int argc, char *argv[]){
    // ./server 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    int optval = 1;
    int ret = setsockopt(sockFd,SOL_SOCKET,SO_REUSEADDR,&optval,sizeof(int));
    ERROR_CHECK(ret,-1,"setsockopt");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    ret = bind(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"bind");
    ret = listen(sockFd,10);
    ERROR_CHECK(ret,-1,"listen");
    int netFd = accept(sockFd,NULL,NULL);
    ERROR_CHECK(netFd,-1,"accept");
    fd_set rdset;
    char buf[4096] = {0};
    while(1){
        FD_ZERO(&rdset);
        FD_SET(STDIN_FILENO,&rdset);
        FD_SET(netFd,&rdset);
        select(netFd+1,&rdset,NULL,NULL,NULL);
        if(FD_ISSET(STDIN_FILENO,&rdset)){
            bzero(buf,sizeof(buf));
            ret = read(STDIN_FILENO,buf,sizeof(buf));
            if(ret == 0){
                send(netFd,"nishigehaoren",13,0);
                break;
            }
            send(netFd,buf,strlen(buf),0);
        }
        if(FD_ISSET(netFd,&rdset)){
            bzero(buf,sizeof(buf));
            ret = recv(netFd,buf,sizeof(buf),0);
            if(ret == 0){
                puts("chat is end!");
                break;
            }
            puts(buf);
        }
    }
    close(sockFd);
    close(netFd);
}
```
$ netstat -an | grep 1234

主动关闭方在四次挥手后会进入TIME_WAIT状态<br />客户端会换端口，服务端因为bind会等待2MSL<br />**服务端**如果**主动关闭**，会进入**TIME_WAIT，固定2MSL（60s)**，服务端在TIME_WAIT状态，不允许bind同样的IP和端口。

<a name="fKtUJ"></a>
#### 让服务端无视TIME_WAIT状态
修改socket的属性（ 在socket创建之后，bind之前）<br />`setsockopt ` -  get and set options on sockets<br />#include <sys/types.h>          /* See NOTES */<br />#include <sys/socket.h><br />int setsockopt(int sockfd, int level, int optname,<br />                      const void *optval, socklen_t optlen);

- int sockfd
- int level
   - SOL_SOCKET  // 传输层socket
- int optname
   - $ man 7 socket  // 查看属性
   - SO_REUSEADDR 
- const void *optval
   - optval  - Aruumet is  an interger boolean flag.
      - 0 假
      - 1 真
- socklen_t optlen
   - optlen  -> sizeof(int)
```c
#include <43func.h>
int main(int argc, char *argv[]){
    // ./server 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    // bind 之前
    int optval = 1;
    int ret = setsockopt(sockFd,SOL_SOCKET,SO_REUSEADDR,&optval,sizeof(int));
    ERROR_CHECK(ret,-1,"setsockopt");
    // bind
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    ret = bind(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"bind");
    ret = listen(sockFd,10);
    ERROR_CHECK(ret,-1,"listen");
    int netFd = accept(sockFd,NULL,NULL);
    ERROR_CHECK(netFd,-1,"accept");
    fd_set rdset;
    char buf[4096] = {0};
    while(1){
        FD_ZERO(&rdset);
        FD_SET(STDIN_FILENO,&rdset);
        FD_SET(netFd,&rdset);
        select(netFd+1,&rdset,NULL,NULL,NULL);
        if(FD_ISSET(STDIN_FILENO,&rdset)){
            bzero(buf,sizeof(buf));
            ret = read(STDIN_FILENO,buf,sizeof(buf));
            if(ret == 0){
                send(netFd,"nishigehaoren",13,0);
                break;
            }
            send(netFd,buf,strlen(buf),0);
        }
        if(FD_ISSET(netFd,&rdset)){
            bzero(buf,sizeof(buf));
            ret = recv(netFd,buf,sizeof(buf),0);
            if(ret == 0){
                puts("chat is end!");
                break;
            }
            puts(buf);
        }
    }
    close(sockFd);
    close(netFd);
}
```

<a name="hgQy2"></a>
#### 如何支持断线重连
用select的rdset 监听服务端的sockFd 读事件<br />如果sockFd就绪，说明服务端调用accept不会阻塞
```c
#include <43func.h>
int main(int argc, char *argv[]){
    // ./client 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    int ret = connect(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"connect");

    fd_set rdset;
    char buf[4096] = {0};
    while(1){
        FD_ZERO(&rdset);
        FD_SET(STDIN_FILENO,&rdset);
        FD_SET(sockFd,&rdset);
        select(sockFd+1,&rdset,NULL,NULL,NULL);
        if(FD_ISSET(STDIN_FILENO,&rdset)){
            bzero(buf,sizeof(buf));
            ret = read(STDIN_FILENO,buf,sizeof(buf));
            if(ret == 0){
                send(sockFd,"nishigehaoren",13,0);
                break;
            }
            send(sockFd,buf,strlen(buf),0);
        }
        if(FD_ISSET(sockFd,&rdset)){
            bzero(buf,sizeof(buf));
            ret = recv(sockFd,buf,sizeof(buf),0);
            if(ret == 0){
                puts("chat is end!");
                break;
            }
            puts(buf);
        }
    }
    close(sockFd);
}
```
```c
#include <43func.h>
int main(int argc, char *argv[]){
    // ./server 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    int optval = 1;
    int ret = setsockopt(sockFd,SOL_SOCKET,SO_REUSEADDR,&optval,sizeof(int));
    ERROR_CHECK(ret,-1,"setsockopt");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    ret = bind(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"bind");
    ret = listen(sockFd,10);
    ERROR_CHECK(ret,-1,"listen");
    // accept要放在select后面
    // 去使用时确保从标准输入中输入数据在客户端建立连接之后
    // accept之后创建新的netFd，这个netFd加入监听--->分离监听和就绪
    // 客户端如果断开连接以后，服务端不要退出，要取消监听netFd
    fd_set rdset;//单纯地去保存就绪的fd
    fd_set monitorSet;//使用一个单独的监听集合
    FD_ZERO(&monitorSet);
    FD_SET(STDIN_FILENO,&monitorSet);
    FD_SET(sockFd,&monitorSet);
    char buf[4096] = {0};
    int netFd = -1;
    while(1){
        memcpy(&rdset,&monitorSet,sizeof(fd_set));
        select(20,&rdset,NULL,NULL,NULL);
        if(FD_ISSET(STDIN_FILENO,&rdset)){
            bzero(buf,sizeof(buf));
            ret = read(STDIN_FILENO,buf,sizeof(buf));
            if(ret == 0){
                send(netFd,"nishigehaoren",13,0);
                break;
            }
            send(netFd,buf,strlen(buf),0);
        }
        if(FD_ISSET(sockFd,&rdset)){
           netFd = accept(sockFd,NULL,NULL);
           ERROR_CHECK(netFd,-1,"accept");
           FD_SET(netFd,&monitorSet);
           puts("new connect is accepted!\n"); 
        }
        if(FD_ISSET(netFd,&rdset)){
            bzero(buf,sizeof(buf));
            ret = read(netFd,buf,sizeof(buf));
            if(ret == 0){
                puts("bye bye");
                FD_CLR(netFd,&monitorSet);
                close(netFd);
                netFd = -1;
                continue;
            }
            puts(buf);
        }
    }
    close(sockFd);
}
```

<a name="edDxl"></a>
#### 聊天室代码
```c
#include <43func.h>
int main(int argc, char *argv[]){
    // ./client 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    int ret = connect(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"connect");

    fd_set rdset;
    char buf[4096] = {0};
    while(1){
        FD_ZERO(&rdset);
        FD_SET(STDIN_FILENO,&rdset);
        FD_SET(sockFd,&rdset);
        select(sockFd+1,&rdset,NULL,NULL,NULL);
        if(FD_ISSET(STDIN_FILENO,&rdset)){
            bzero(buf,sizeof(buf));
            ret = read(STDIN_FILENO,buf,sizeof(buf));
            if(ret == 0){
                send(sockFd,"nishigehaoren",13,0);
                break;
            }
            send(sockFd,buf,strlen(buf),0);
        }
        if(FD_ISSET(sockFd,&rdset)){
            bzero(buf,sizeof(buf));
            ret = recv(sockFd,buf,sizeof(buf),0);
            if(ret == 0){
                puts("chat is end!");
                break;
            }
            puts(buf);
        }
    }
    close(sockFd);
}
```
```c
#include <43func.h>
int main(int argc, char *argv[]){
    // ./server 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    int optval = 1;
    int ret = setsockopt(sockFd,SOL_SOCKET,SO_REUSEADDR,&optval,sizeof(int));
    ERROR_CHECK(ret,-1,"setsockopt");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    ret = bind(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"bind");
    ret = listen(sockFd,10);
    ERROR_CHECK(ret,-1,"listen");
    fd_set rdset;//单纯地去保存就绪的fd
    fd_set monitorSet;//使用一个单独的监听集合
    FD_ZERO(&monitorSet);
    FD_SET(sockFd,&monitorSet);
    char buf[4096] = {0};
    int netFdArr[10] = {0};
    int curConn = 0;
    //实现聊天室
    //服务端不再监听标准输入 
    //服务端的功能：处理新的客户端 监听sockFd
    //处理客户端发送的消息的转发 维护netFd的数组然后进行转发
    while(1){
        memcpy(&rdset,&monitorSet,sizeof(fd_set));
        select(20,&rdset,NULL,NULL,NULL);
        if(FD_ISSET(sockFd,&rdset)){
           netFdArr[curConn]= accept(sockFd,NULL,NULL);
           ERROR_CHECK(netFdArr[curConn],-1,"accept");
           FD_SET(netFdArr[curConn],&monitorSet);
           printf("new connect is accepted!,curConn = %d\n", curConn);
           ++curConn; 
        }
        for(int i = 0;i < curConn; ++i){
            if(FD_ISSET(netFdArr[i],&rdset)){
                bzero(buf,sizeof(buf));
                recv(netFdArr[i],buf,sizeof(buf),0);
                for(int j = 0; j < curConn; ++j){
                    if(j == i){
                        continue;
                    }
                    send(netFdArr[j],buf,strlen(buf),0);
                }
            }
        }
    }
    close(sockFd);
}

```


<a name="Gtkyn"></a>
#### 使用UDP聊天
客户端必须**先 sendto ** （ **服务端的IP和port 固定**）<br />ssize_t sendto(int sockfd, const void *buf, size_t len, int flags, const struct sockaddr *dest_addr, socklen_t addrlen); <br />ssize_t recvfrom(int sockfd, void *buf, size_t len, int flags, struct sockaddr *src_addr, socklen_t *addrlen);  

- socklen_t *addrlen   	传入传出参数
```c
#include <43func.h>
int main(int argc, char *argv[]){
    // ./client 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_DGRAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    // int ret = sendto(sockFd,"hello udp",9,0,(struct sockaddr*)&addr, sizeof(addr));
    // ERROR_CHECK(ret,-1,"sendto");
    // 
    // socklen_t sockLen = sizeof(addr);   
    // char buf[4096] = {0};
    // ret = recvfrom(sockFd,buf,4096,0,(struct sockaddr*)&addr, &sockLen);
    // ERROR_CHECK(ret,-1,"recvfrom");
    // puts(buf);
    sendto(sockFd,"nihao",5,0,(struct sockaddr *)&addr,sizeof(addr));
    sendto(sockFd,"huaidan",7,0,(struct sockaddr *)&addr,sizeof(addr));
   
    close(sockFd);
}
```
```c
#include <43func.h>
int main(int argc, char *argv[]){
    // ./server 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_DGRAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    int ret = bind(sockFd,(struct sockaddr *)&addr, sizeof(addr));
    ERROR_CHECK(ret,-1,"bind");
    
    // 服务端 先recvfrom 后sendto
    struct sockaddr_in clientAddr;
    socklen_t clientAddrLen = sizeof(clientAddr);
    //char buf[1024] = {0};
    char buf[4] = {0};
    ret = recvfrom(sockFd,buf,sizeof(buf),0,(struct sockaddr *)&clientAddr, &clientAddrLen);
    ERROR_CHECK(ret,-1,"recvfrom");
    printf("buf = %s\n", buf);
    printf("client ip = %s\n", inet_ntoa(clientAddr.sin_addr));
    printf("client port = %d\n", ntohs(clientAddr.sin_port));
    // ret = sendto(sockFd,"nihao udp",9,0,(struct sockaddr *)&clientAddr,clientAddrLen);
    // ERROR_CHECK(ret,-1,"sendto");
    
    bzero(buf,sizeof(buf));
    ret = recvfrom(sockFd,buf,sizeof(buf),0,(struct sockaddr *)&clientAddr, &clientAddrLen);
    ERROR_CHECK(ret,-1,"recvfrom");
    printf("buf = %s\n", buf);
    
    close(sockFd);
}
```
TCP和UDP区别：<br />UDP会保留消息边界，超出则丢失<br />TCP不会保留消息边界，会分包

TCP和UDP收到消息个数的区别：<br /> 主机A向主机B发送数据，以TCP方式发送3个包，对方可能会收到几个包？<br />答：任意个 <br />以UDP方式发送3个包，对方可能会收到几个包？<br />答：至多3个  


```c
#include <43func.h>
int main(int argc, char *argv[]){
    // ./client_chat 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_DGRAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    char buf[1024] = "I am comming";
    int ret = sendto(sockFd,buf,strlen(buf),0,(struct sockaddr *)&addr, sizeof(addr));
    ERROR_CHECK(ret,-1,"recvfrom");
    fd_set rdset;
    while(1){
        FD_ZERO(&rdset);
        FD_SET(STDIN_FILENO,&rdset);
        FD_SET(sockFd,&rdset);
        select(sockFd+1,&rdset,NULL,NULL,NULL);
        puts("select returns");
        if(FD_ISSET(STDIN_FILENO,&rdset)){
            bzero(buf,sizeof(buf));
            int ret = read(STDIN_FILENO,buf,sizeof(buf));
            if(ret == 0){
                // 退出 发送0数据包
                sendto(sockFd,buf,0,0,(struct sockaddr *)&addr,sizeof(addr));
                break;
            }
            sendto(sockFd,buf,strlen(buf),0,(struct sockaddr *)&addr,sizeof(addr));
        }
        if(FD_ISSET(sockFd,&rdset)){
            bzero(buf,sizeof(buf));
            socklen_t addrLen = sizeof(addr);
            int ret = recvfrom(sockFd,buf,sizeof(buf),0,(struct sockaddr *)&addr,&addrLen);
            if(ret == 0){
                break;
            }
            puts(buf);
        }
    } 
    close(sockFd);
}
```
```c
#include <43func.h>
int main(int argc, char *argv[]){
    // ./server_chat 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_DGRAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    int ret = bind(sockFd,(struct sockaddr *)&addr, sizeof(addr));
    ERROR_CHECK(ret,-1,"bind");
    struct sockaddr_in clientAddr;
    socklen_t clientAddrLen = sizeof(clientAddr);
    char buf[1024] = {0};
    ret = recvfrom(sockFd,buf,sizeof(buf),0,(struct sockaddr *)&clientAddr, &clientAddrLen);
    ERROR_CHECK(ret,-1,"recvfrom");
    printf("buf = %s\n", buf);
    printf("client ip = %s\n", inet_ntoa(clientAddr.sin_addr));
    printf("client port = %d\n", ntohs(clientAddr.sin_port));
    fd_set rdset;
    while(1){
        FD_ZERO(&rdset);
        FD_SET(STDIN_FILENO,&rdset);
        FD_SET(sockFd,&rdset);
        select(sockFd+1,&rdset,NULL,NULL,NULL);
        puts("select returns");
        if(FD_ISSET(STDIN_FILENO,&rdset)){
            bzero(buf,sizeof(buf));
            int ret = read(STDIN_FILENO,buf,sizeof(buf));
            if(ret == 0){
                // 退出 发送0数据包
                sendto(sockFd,buf,0,0,(struct sockaddr *)&clientAddr,clientAddrLen);
                break;
            }
            sendto(sockFd,buf,strlen(buf),0,(struct sockaddr *)&clientAddr,clientAddrLen);
        }
        if(FD_ISSET(sockFd,&rdset)){
            bzero(buf,sizeof(buf));
            int ret = recvfrom(sockFd,buf,sizeof(buf),0,(struct sockaddr *)&clientAddr,&clientAddrLen);
            if(ret == 0){
                break;
            }
            puts(buf);
        }
    } 
    close(sockFd);
}
```

<a name="Z4lWm"></a>
### epoll ( Linux 下 )
**select的缺陷：（面试必考）**

1. fdset的本质是一个位图。容量固定1024

扩容fd_set, 需要修改 内核

2. 监听和就绪用的同一个数据结构

使用困难

3. 用户定义的 fd_set 需要**拷贝**到内核的 fd_set 

存在多次大量（1024）从用户态到内核态的拷贝。

4. 采用**轮询**找到就绪的fd

在大量连接但少量就绪的情况下，浪费大量时间

高并发服务器的基石 epoll<br />高性能`I/O`多路复用

1. 把所有的监听数据放在内核态，成一个文件对象（**避免拷贝问题**）

监听集合无大小限制<br />红黑树 数据结构<br />![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650685762233-dfbb13bb-9129-41b1-9d5b-e0bdca7002ce.png#clientId=uc055a1f1-cc9f-4&crop=0&crop=0&crop=1&crop=1&from=paste&height=181&id=uf7257484&margin=%5Bobject%20Object%5D&name=image.png&originHeight=249&originWidth=498&originalType=binary&ratio=1&rotation=0&showTitle=false&size=89088&status=done&style=none&taskId=u9d5ecf17-6a0f-4bab-9b37-6b9ba7cb7b2&title=&width=362.1818181818182)

2. 监听和就绪分离

就绪事件用**一个单独的就绪事件队列**保存<br />只需 将**就绪事件队列**从内核态**拷贝**到用户态<br />优点：

1. 方便使用
1. 大量连接但少量就绪的情况下，性能大幅提升。

`epoll` - I/O event notification facility.<br />#include <sys/epoll.h>

步骤：

1. 创建一个文件对象
1. 设置监听
1. 陷入阻塞，直到任一监听就绪
1. 遍历就绪事件队列，处理事件

$ man epoll_create<br />`epoll_create` - open an epoll file descriptor<br />#include <sys/epoll.h><br />int epoll_create(int size);
```c
#include <43func.h>
int main(){
    int epfd = epoll_create(1);
    ERROR_CHECK(epfd,-1,"epoll_create");
    printf("epfd = %d\n", epfd);
    close(epfd);
}
```

`epoll_ctl`- control interface for an epoll file descriptor<br />#include <sys/epoll.h><br />int epoll_ctl(int epfd, int op, int fd, struct epoll_event *event);

- int epfd
- int op
   -  EPOLL_CTL_ADD    
   - EPOLL_CTL_MOD 
   - EPOLL_CTL_DEL
- int fd
- struct epoll_event *event
```c
typedef union epoll_data {
      void        *ptr;
      int          fd;
      uint32_t     u32;
      uint64_t     u64;
} epoll_data_t;

struct epoll_event {
      uint32_t     events;      /* Epoll events */
      epoll_data_t data;        /* User data variable */
};
```

`epoll_wait` - wait for an I/O event on an epoll file descriptor<br />#include <sys/epoll.h><br />int epoll_wait(int epfd, struct epoll_event *events,<br />                      int maxevents, int timeout);

- struct epoll_event *events
   - 用户给就绪事件分配的内存的地址
   - 用户创建的一维数组 data.fd
-  int maxevents
   - 数组长度
- int timeout
   - 超时时间 - 等待毫秒数
   - -1 表示无限等待

epoll_wait的返回值是就绪事件的个数
```c
#include <43func.h>
int main(int argc, char *argv[]){
    // ./server 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    int optval = 1;
    int ret = setsockopt(sockFd,SOL_SOCKET,SO_REUSEADDR,&optval,sizeof(int));
    ERROR_CHECK(ret,-1,"setsockopt");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    ret = bind(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"bind");
    ret = listen(sockFd,10);
    ERROR_CHECK(ret,-1,"listen");
    int netFd = accept(sockFd,NULL,NULL);
    ERROR_CHECK(netFd,-1,"accept");
    int epfd = epoll_create(1);//创建epoll文件对象
    ERROR_CHECK(epfd,-1,"epoll_create");
    struct epoll_event event;
    event.data.fd = STDIN_FILENO;
    event.events = EPOLLIN; 
    epoll_ctl(epfd,EPOLL_CTL_ADD,STDIN_FILENO,&event);//把stdin以读事件加入监听
    event.data.fd = netFd;
    event.events = EPOLLIN; 
    epoll_ctl(epfd,EPOLL_CTL_ADD,netFd,&event);//把已连接socket以读事件加入监听
    char buf[4096] = {0};
    struct epoll_event readyArr[2];
    while(1){
        int readyNum = epoll_wait(epfd,readyArr,2,-1);//epoll_wait的返回值是就绪事件的个数
        puts("epoll_wait returns");
        for(int i = 0; i < readyNum; ++i){
            if(readyArr[i].data.fd == STDIN_FILENO){
                bzero(buf,sizeof(buf));
                int ret = read(STDIN_FILENO,buf,sizeof(buf));
                if(ret == 0){
                    goto end;
                }
                send(netFd,buf,strlen(buf),0);
            }
            else if(readyArr[i].data.fd == netFd){
                bzero(buf,sizeof(buf));
                int ret = recv(netFd,buf,sizeof(buf),0);
                if(ret == 0){
                    goto end;
                }
                puts(buf);
            }
        }
    }
end:
    close(netFd);
    close(epfd);
    close(sockFd);
}
```
```c
while(1){
    int flag = 0;
    for(int i = 0; i < N; ++i){
        ...
        ...
        if(){
            flag = 1;
            break;
        }
        ...
        ...
    }
    if(flag == 1)
        break;
    ...
}
```


服务端超时关闭 和 支持多个客户端连接
```c
#include <43func.h>
int main(int argc, char *argv[]){
    // ./server 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    int optval = 1;
    int ret = setsockopt(sockFd,SOL_SOCKET,SO_REUSEADDR,&optval,sizeof(int));
    ERROR_CHECK(ret,-1,"setsockopt");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    ret = bind(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"bind");
    ret = listen(sockFd,10);
    ERROR_CHECK(ret,-1,"listen");
    int netFd = accept(sockFd,NULL,NULL);
    ERROR_CHECK(netFd,-1,"accept");
    int epfd = epoll_create(1);//创建epoll文件对象
    ERROR_CHECK(epfd,-1,"epoll_create");
    struct epoll_event event;
    event.data.fd = STDIN_FILENO;
    event.events = EPOLLIN; 
    epoll_ctl(epfd,EPOLL_CTL_ADD,STDIN_FILENO,&event);//把stdin以读事件加入监听
    event.data.fd = netFd;
    event.events = EPOLLIN; 
    epoll_ctl(epfd,EPOLL_CTL_ADD,netFd,&event);//把已连接socket以读事件加入监听
    char buf[4096] = {0};
    struct epoll_event readyArr[2];
    while(1){
        int readyNum = epoll_wait(epfd,readyArr,2,-1);//epoll_wait的返回值是就绪事件的个数
        puts("epoll_wait returns");
        for(int i = 0; i < readyNum; ++i){
            if(readyArr[i].data.fd == STDIN_FILENO){
                bzero(buf,sizeof(buf));
                int ret = read(STDIN_FILENO,buf,sizeof(buf));
                if(ret == 0){
                    goto end;
                }
                send(netFd,buf,strlen(buf),0);
            }
            else if(readyArr[i].data.fd == netFd){
                bzero(buf,sizeof(buf));
                int ret = recv(netFd,buf,sizeof(buf),0);
                if(ret == 0){
                    goto end;
                }
                puts(buf);
            }
        }
    }
end:
    close(netFd);
    close(epfd);
    close(sockFd);
}
```

recv和read的非阻塞

- 当缓冲区中有数据，正常取出
- 当缓冲区中无数据，直接返回-1  （ 好处： 进程不会切换状态）

适用于  持续但不连续（ 网络 ）

- **_异步非阻塞方式 （包括 信号驱动 ）_**
   - **_Linux  IO-uring_**
   - **_Windows icop_**

![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650856326558-85485684-0e6e-4bfe-ae31-f69267241250.png#clientId=u8734371d-992d-4&crop=0&crop=0&crop=1&crop=1&from=paste&height=97&id=u11989b65&margin=%5Bobject%20Object%5D&name=image.png&originHeight=134&originWidth=211&originalType=binary&ratio=1&rotation=0&showTitle=false&size=24459&status=done&style=none&taskId=u8657d6fb-f47a-493d-8f81-18f3f76305a&title=&width=153.45454545454547)

- 同步阻塞

![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650856478630-3a8c3835-a7df-4c9d-b8ea-30c3fad89550.png#clientId=u8734371d-992d-4&crop=0&crop=0&crop=1&crop=1&from=paste&height=124&id=u7b2eb598&margin=%5Bobject%20Object%5D&name=image.png&originHeight=170&originWidth=317&originalType=binary&ratio=1&rotation=0&showTitle=false&size=24584&status=done&style=none&taskId=u18c51b02-4958-4713-a15d-25022148fd5&title=&width=230.54545454545453)

- 同步非阻塞

![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650856492550-0f2d1fbc-4bbe-43b0-94e0-8bbcb8cc963d.png#clientId=u8734371d-992d-4&crop=0&crop=0&crop=1&crop=1&from=paste&height=158&id=u4a3a1165&margin=%5Bobject%20Object%5D&name=image.png&originHeight=217&originWidth=395&originalType=binary&ratio=1&rotation=0&showTitle=false&size=52904&status=done&style=none&taskId=ubb1ef33b-82bf-4ca5-9c74-d0eb7c68880&title=&width=287.27272727272725)

-  IO多路复用  （ + 非阻塞  目前主流服务器）
- ![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650856601682-9047bb8d-808f-4c05-8d85-5d8cdbee149a.png#clientId=u8734371d-992d-4&crop=0&crop=0&crop=1&crop=1&from=paste&height=163&id=u7397ded8&margin=%5Bobject%20Object%5D&name=image.png&originHeight=224&originWidth=434&originalType=binary&ratio=1&rotation=0&showTitle=false&size=53590&status=done&style=none&taskId=u0c46a51c-4b67-499f-bb83-19bf25e36eb&title=&width=315.6363636363636)

既可配合阻塞，也可配合非阻塞

修改文件描述符的阻塞与非阻塞<br />`fcntl `- manipulate file descriptor<br />#include <unistd.h><br />#include <fcntl.h><br /> int fcntl(int fd, int cmd, ... /* arg */ );

- int cmd
   - F_GETFL (void)
   -  F_SETFL (int)  + status
```c
#include <43func.h>
int setNonblock(int fd){
    int status = fcntl(fd,F_GETFL);
    status |= O_NONBLOCK;//既要原来的属性也要O_NONBLOCK
    int ret = fcntl(fd,F_SETFL,status);
    ERROR_CHECK(ret,-1,"fcntl");
    return 0;
}
int main(int argc,char *argv[]){
    // ./server 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    //创建一个socket 支持IPv4和TCP
    ERROR_CHECK(sockFd,-1,"socket");
    printf("sockFd = %d\n", sockFd);
    struct sockaddr_in addr;//创建时 用 sockaddr_in
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    //调用函数时，先取地址再强转
    //对于bind而言 addr一定是本地地址
    int ret = bind(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"bind");
    ret = listen(sockFd,10);
    ERROR_CHECK(ret,-1,"listen");
    struct sockaddr_in clientAddr;
    //socklen_t clientAddrLen = /*sizeof(clientAddr)*/ 0;//写0是会出问题
    socklen_t clientAddrLen = sizeof(clientAddr);//写0是会出问题
    int netFd = accept(sockFd,(struct sockaddr *)&clientAddr,&clientAddrLen);
    ERROR_CHECK(netFd,-1,"accept");
    printf("netFd = %d\n", netFd);
    printf("client addr length = %d\n", clientAddrLen);
    printf("client family = %d\n", clientAddr.sin_family);
    printf("client port = %d\n", ntohs(clientAddr.sin_port));
    printf("client ip = %s\n",inet_ntoa(clientAddr.sin_addr));
    char buf[4096] = {0};
    //setNonblock(netFd);
    ret = recv(netFd,buf,sizeof(buf),MSG_DONTWAIT);
    if(ret == -1){
        puts("no resource now!\n");
    }
    ret = recv(netFd,buf,sizeof(buf),0);
    if(ret == -1){
        puts("no resource now!\n");
    }
    //ERROR_CHECK(ret,-1,"recv");
    // while(1){
    //     ret = recv(netFd,buf,sizeof(buf),MSG_DONTWAIT);
    //     //临时让本次recv是非阻塞的
    //     if(ret == -1){
    //         puts("no resource now!\n");
    //         sleep(1);
    //     }
    //     else{
    //         break;
    //     }
    // }
    close(sockFd);
}
```
```c
#include <43func.h>
int main(int argc,char *argv[]){
    // ./server 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    //创建一个socket 支持IPv4和TCP
    ERROR_CHECK(sockFd,-1,"socket");
    printf("sockFd = %d\n", sockFd);
    struct sockaddr_in addr;//创建时 用 sockaddr_in
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    int ret = connect(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"connect");
    //sleep(10);
    send(sockFd,"hello",5,0);
    sleep(10);
    close(sockFd);
}
```
工作常用方法：<br />MSG_DONTWAIT  // 临时设置一次**recv 非阻塞**

epoll的触发方式

- 水平触发

![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650858303844-a3d067a1-2f6d-4537-b52a-beec2ee041da.png#clientId=u8734371d-992d-4&crop=0&crop=0&crop=1&crop=1&from=paste&height=131&id=u45353d45&margin=%5Bobject%20Object%5D&name=image.png&originHeight=180&originWidth=478&originalType=binary&ratio=1&rotation=0&showTitle=false&size=69365&status=done&style=none&taskId=u28b1b620-829a-495d-950b-e065644aa72&title=&width=347.6363636363636)<br />某一个客户有大量数据，epoll会重复就绪

- 边缘触发

![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650858316938-0c1e6de3-15d8-4ae7-b77b-de95a16d086a.png#clientId=u8734371d-992d-4&crop=0&crop=0&crop=1&crop=1&from=paste&height=120&id=u4b3f8d07&margin=%5Bobject%20Object%5D&name=image.png&originHeight=165&originWidth=467&originalType=binary&ratio=1&rotation=0&showTitle=false&size=49972&status=done&style=none&taskId=ude126404-95fd-476d-803f-5f39fec54fc&title=&width=339.6363636363636)

如何实现既要epoll就绪次数少，又要把数据读完？<br />用**epoll的边缘触发** + **while(){ 非阻塞recv; }    （ 面试常面 ）**
```c
#include <43func.h>
int main(int argc, char *argv[]){
    // ./server 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    int optval = 1;
    int ret = setsockopt(sockFd,SOL_SOCKET,SO_REUSEADDR,&optval,sizeof(int));
    ERROR_CHECK(ret,-1,"setsockopt");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    ret = bind(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"bind");
    ret = listen(sockFd,10);
    ERROR_CHECK(ret,-1,"listen");
    int netFd = accept(sockFd,NULL,NULL);
    ERROR_CHECK(netFd,-1,"accept");
    int epfd = epoll_create(1);//创建epoll文件对象
    ERROR_CHECK(epfd,-1,"epoll_create");
    struct epoll_event event;
    event.data.fd = STDIN_FILENO;
    event.events = EPOLLIN; 
    epoll_ctl(epfd,EPOLL_CTL_ADD,STDIN_FILENO,&event);//把stdin以读事件加入监听
    event.data.fd = netFd;
    //event.events = EPOLLIN; 
    event.events = EPOLLIN|EPOLLET; 
    epoll_ctl(epfd,EPOLL_CTL_ADD,netFd,&event);//把已连接socket以读事件加入监听
    char buf[5] = {0};
    struct epoll_event readyArr[2];
    while(1){
        int readyNum = epoll_wait(epfd,readyArr,2,-1);//epoll_wait的返回值是就绪事件的个数
        puts("epoll_wait returns");
        for(int i = 0; i < readyNum; ++i){
            if(readyArr[i].data.fd == STDIN_FILENO){
                bzero(buf,sizeof(buf));
                int ret = read(STDIN_FILENO,buf,sizeof(buf)-1);
                if(ret == 0){
                    goto end;
                }
                send(netFd,buf,strlen(buf),0);
            }
            else if(readyArr[i].data.fd == netFd){
                bzero(buf,sizeof(buf));
                //int ret = recv(netFd,buf,sizeof(buf)-1,0);
                //if(ret == 0){
                //    goto end;
                //}
                //puts(buf);
                int ret;
                while(1){
                    bzero(buf,sizeof(buf));
                    ret = recv(netFd,buf,sizeof(buf)-1,MSG_DONTWAIT);
                    puts(buf);
                    if(ret == 0 || ret == -1){
                        break;
                    }
                }
            }
        }
    }
end:
    close(netFd);
    close(epfd);
    close(sockFd);
}
```


设置socket的属性<br />$ man 7 socket<br /> int setsockopt(int sockfd, int level, int optname, const void *optval, socklen_t optlen); 

- const void *optval  属性首地址
   - SO_RCVBUF
   - SO_SNDBUF
   - SO_RCVLOWAT  修改缓冲区下限
- socklen_t optlen   获取属性长度

int getsockopt(int sockfd, int level, int optname, void *optval, socklen_t *optlen);  

- socklen_t *optlen   属性的长度
```c
#include <43func.h>
int main(){
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    int bufSize;
    socklen_t len = sizeof(bufSize);
    int ret = getsockopt(sockFd,SOL_SOCKET,SO_SNDBUF,&bufSize,&len);
    ERROR_CHECK(ret,-1,"getsockopt");
    printf("bufSize = %d, len = %d\n", bufSize,len);
    bufSize = 4096;
    ret = setsockopt(sockFd,SOL_SOCKET,SO_SNDBUF,&bufSize,len);
    ERROR_CHECK(ret,-1,"setsockopt");
    ret = getsockopt(sockFd,SOL_SOCKET,SO_SNDBUF,&bufSize,&len);
    ERROR_CHECK(ret,-1,"getsockopt");
    printf("bufSize = %d, len = %d\n", bufSize,len);
}
```


```c
#include <43func.h>
int main(int argc, char *argv[]){
    // ./client 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    int ret = connect(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"connect");

    fd_set rdset;
    char buf[4096] = {0};
    while(1){
        FD_ZERO(&rdset);
        FD_SET(STDIN_FILENO,&rdset);
        FD_SET(sockFd,&rdset);
        select(sockFd+1,&rdset,NULL,NULL,NULL);
        if(FD_ISSET(STDIN_FILENO,&rdset)){
            bzero(buf,sizeof(buf));
            ret = read(STDIN_FILENO,buf,sizeof(buf));
            if(ret == 0){
                send(sockFd,"nishigehaoren",13,0);
                break;
            }
            send(sockFd,buf,strlen(buf),0);
        }
        if(FD_ISSET(sockFd,&rdset)){
            int lowat = 5;
            setsockopt(sockFd,SOL_SOCKET,SO_RCVLOWAT,&lowat,sizeof(lowat));
            bzero(buf,sizeof(buf));
            ret = recv(sockFd,buf,sizeof(buf),0);
            if(ret == 0){
                puts("chat is end!");
                break;
            }
            puts(buf);
        }
    }
    close(sockFd);
}
```

recv MSG_PEEK属性 <br />复制缓冲区数据，但是不取出
```c
#include <43func.h>
int setNonblock(int fd){
    int status = fcntl(fd,F_GETFL);
    status |= O_NONBLOCK;//既要原来的属性也要O_NONBLOCK
    int ret = fcntl(fd,F_SETFL,status);
    ERROR_CHECK(ret,-1,"fcntl");
    return 0;
}
int main(int argc,char *argv[]){
    // ./server 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    //创建一个socket 支持IPv4和TCP
    ERROR_CHECK(sockFd,-1,"socket");
    printf("sockFd = %d\n", sockFd);
    struct sockaddr_in addr;//创建时 用 sockaddr_in
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    //调用函数时，先取地址再强转
    //对于bind而言 addr一定是本地地址
    int ret = bind(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"bind");
    ret = listen(sockFd,10);
    ERROR_CHECK(ret,-1,"listen");
    struct sockaddr_in clientAddr;
    //socklen_t clientAddrLen = /*sizeof(clientAddr)*/ 0;//写0是会出问题
    socklen_t clientAddrLen = sizeof(clientAddr);//写0是会出问题
    int netFd = accept(sockFd,(struct sockaddr *)&clientAddr,&clientAddrLen);
    ERROR_CHECK(netFd,-1,"accept");
    printf("netFd = %d\n", netFd);
    printf("client addr length = %d\n", clientAddrLen);
    printf("client family = %d\n", clientAddr.sin_family);
    printf("client port = %d\n", ntohs(clientAddr.sin_port));
    printf("client ip = %s\n",inet_ntoa(clientAddr.sin_addr));
    char buf[4096] = {0};
    bzero(buf,sizeof(buf));
    // 复制缓冲区数据，但是不取出
    ret = recv(netFd,buf,sizeof(buf),MSG_PEEK);
    puts(buf);
    bzero(buf,sizeof(buf));
    ret = recv(netFd,buf,sizeof(buf),0);
    puts(buf);
    close(sockFd);
}
```


<a name="dfsbQ"></a>
### `epoll`总结
| select | epoll |
| --- | --- |
| fd_set rdset;<br />FD_ZERO(&rdset); // 放在循环内 | int epfd = epoll_create(1); <br />// 放在循环外 |
| <br /><br />FD_SET(fd, &rdset); // 放在循环内<br />不能直接用于就绪后的事件，需单独设置monitorSet | struct epoll_event event;<br />event.data.fd = fd;<br />event.events = EPOLLIN;<br />epoll_ctl(...,EPOLL_CTL_ADD,...)<br />可以直接加新的监听fd |
| <br /><br />用FD_ISSET( )轮询 | for(  ){  // 遍历就绪事件队列<br />readyArr[i].data.fd <br />} |


epoll实现server_room
```c
#include <43func.h>
int main(int argc, char *argv[]){
    // ./client 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    int ret = connect(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"connect");

    fd_set rdset;
    char buf[4096] = {0};
    while(1){
        FD_ZERO(&rdset);
        FD_SET(STDIN_FILENO,&rdset);
        FD_SET(sockFd,&rdset);
        select(sockFd+1,&rdset,NULL,NULL,NULL);
        if(FD_ISSET(STDIN_FILENO,&rdset)){
            bzero(buf,sizeof(buf));
            ret = read(STDIN_FILENO,buf,sizeof(buf));
            if(ret == 0){
                send(sockFd,"nishigehaoren",13,0);
                break;
            }
            send(sockFd,buf,strlen(buf),0);
        }
        if(FD_ISSET(sockFd,&rdset)){
            bzero(buf,sizeof(buf));
            ret = recv(sockFd,buf,sizeof(buf),0);
            if(ret == 0){
                puts("chat is end!");
                break;
            }
            puts(buf);
        }
    }
    close(sockFd);
}
```
```c
#include <43func.h>
int main(int argc, char *argv[]){
    // ./server 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    int optval = 1;
    int ret = setsockopt(sockFd,SOL_SOCKET,SO_REUSEADDR,&optval,sizeof(int));
    ERROR_CHECK(ret,-1,"setsockopt");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    ret = bind(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"bind");
    ret = listen(sockFd,10);
    ERROR_CHECK(ret,-1,"listen");
    int epfd = epoll_create(1);
    struct epoll_event event;
    event.data.fd = sockFd;
    event.events = EPOLLIN;
    epoll_ctl(epfd,EPOLL_CTL_ADD,sockFd,&event);
    char buf[4096] = {0};
    int netFdArr[10] = {0};
    int curConn = 0;
    //实现聊天室
    //服务端不再监听标准输入 
    //服务端的功能：处理新的客户端 监听sockFd
    //处理客户端发送的消息的转发 维护netFd的数组然后进行转发
    struct epoll_event readyArr[11] = {0};
    while(1){
        int readyNum = epoll_wait(epfd,readyArr,11,-1);
        puts("epoll_wait returns!");
        for(int i = 0; i < readyNum; ++i){
            //遍历就绪事件队列
            if(readyArr[i].data.fd == sockFd){
                netFdArr[curConn]= accept(sockFd,NULL,NULL);
                ERROR_CHECK(netFdArr[curConn],-1,"accept");
                event.data.fd = netFdArr[curConn];
                event.events = EPOLLIN;
                epoll_ctl(epfd,EPOLL_CTL_ADD,netFdArr[curConn],&event);
                printf("new connect is accepted!,curConn = %d\n", curConn);
                ++curConn; 
            }
            else{
                bzero(buf,sizeof(buf));
                recv(readyArr[i].data.fd,buf,sizeof(buf),0);
                for(int j = 0; j < curConn; ++j){
                    if(readyArr[i].data.fd == netFdArr[j]){
                        continue;
                    }
                    send(netFdArr[j],buf,strlen(buf),0);
                }
            }
        }
    }
    close(sockFd);
}
```
