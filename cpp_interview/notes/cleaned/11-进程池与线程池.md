设计架构：

1. 高性能

充分利用操作系统提供的资源 ， 能 不浪费就不浪费

2. 可维护性

抽象

设计方案

1. 基于线程

每当有一个客户端连接时，创建一个线程；断开时，销毁

- 优点：维护性很高     	 _Apache_
- 缺点：性能差   浪费大量资源创建和销毁 线程（进程）
2. 池化的思路

申请了资源之后，使用完毕不用马上归还，可以交给其他进程复用

进程池和线程池设计思路

1. 提前创建号若干个进程
2. 每当有任务到来时分配一个进程
2. 任务完成后归还进程
2. 整个进程池关闭的时候再销毁

问题：

- 任务太多   任务队列
- 事件   IO多路复用

事件驱动模型  event driven
优点：性能更高

## 进程池
worker  process ：工作进程
```c
while(1){
    取任务
    完成任务
    恢复资源
}
event loop  事件循环
```

master  process

1. 创建子进程
1. 监听子进程
1. tcp bind / listen     监听sockFd

进程池初步项目 是 实现一个下载服务器

父子进程移交连接
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650894626622-8cfd531f-69db-4229-a397-2f7233b20c44.png)
解决办法1：
先socket再fork，共享一个sockFd，有可能一次connect使多个accept就绪
”惊群“  SO_REUSEPORT  nginx
解决办法2：
先fork( ) 再 socket
先fork( ) 后，怎么让父子进程共享文件对象？

在两个进程间传递文件对象
ssize_t sendmsg(int sockfd, const struct msghdr *msg, int flags);

- int sockfd     不能是管道，必须是socket
- const struct msghdr *msg
```c
struct msghdr {
     void         *msg_name;       /* Optional address */
     socklen_t     msg_namelen;    /* Size of address */
     struct iovec *msg_iov;        /* Scatter/gather array */
     size_t        msg_iovlen;     /* # elements in msg_iov */
     void         *msg_control;    /* Ancillary data, see below */
     size_t        msg_controllen; /* Ancillary data buffer len */
     int           msg_flags;      /* Flags (unused) */
};
// msg_iovlen  为1时  退化为一个连续的消息
```
文本信息的处理
```c
struct iovec{
    void *iov_base;		/* Starting address */
    size_t iv_len:		/* Number of bytes to transfer */
}
```
控制信息的处理
```c
struct cmsghdr{
    size_t cmsg_len;	/* Data byte count, including header
                           ( type is socklen_t in POSIX */
    int cmsg_level;		/* Originating protocol */
    int cmsg_type;		/* Protocol-specific type */
/* followed by
   unsiged char cmsg_data[];*/
最后一行说明 C语言中的变长数组，不能在栈上分配
};

unsiged char *CMSG_DATA(struct cmsghdr *cmsg);
// 返回承载数据的起始地址

(int *)CMSG_DATA() = fd // 先获取地址，再转成int *,再间接访问赋值
```

      1. 申请内存（堆上） struct smsghdr
         - cmsg_len  是文件描述符
         - SOL_SOCKET
         - SCM_RIGHTS // socket control message

除了可以传递文本内容，还可以传递控制信息

- unsigned char * SMSG_DATA(struct smsghdr *smsg )
- int* CMSG_DATA = fd; 先获取地址，再转成int *,再间接访问赋值

int socketpair(int domain, int type, int  protocol, int sv[2]);
socketpair  sv[2]   // 参考父子进程间pipe的使用
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650876682187-f645973f-4734-4725-bfa3-b474c3e44583.png)

- socketpair  sv[0] 和sv[0]  既可读，又可写
- 第一部分发送文本信息，第二部分发送文件描述符
```c
int sendFd(int pipeFd, int fdToSend){
    struct msghdr hdr;
    bzero(&hdr,sizeof(hdr));
    //第一个部分发送文本数据
    struct iovec iov[1];//长度为1时，离散数据退化成连续
    char buf[10] = "hello";
    iov[0].iov_base = buf;
    iov[0].iov_len = 5;
    hdr.msg_iov = iov;
    hdr.msg_iovlen = 1;
    //第二个部分发送控制数据
    struct cmsghdr *pcmsghdr = (struct cmsghdr *)calloc(1,sizeof(struct cmsghdr));
    pcmsghdr->cmsg_len = CMSG_LEN(sizeof(int));//要承载的控制信息是一个整型数据
    pcmsghdr->cmsg_level = SOL_SOCKET;
    pcmsghdr->cmsg_type = SCM_RIGHTS;
    *(int *)CMSG_DATA(pcmsghdr) = fdToSend;
    hdr.msg_control = pcmsghdr;
    hdr.msg_controllen = CMSG_LEN(sizeof(int));
    int ret = sendmsg(pipeFd,&hdr,0);
    ERROR_CHECK(ret,-1,"sendmsg");
    return 0;
}
```
```c
int recvFd(int pipeFd, int *pFdToRecv){
    struct msghdr hdr;
    bzero(&hdr,sizeof(hdr));
    //第一个部分发送文本数据
    struct iovec iov[1];//长度为1时，离散数据退化成连续
    char buf[10] = {0};
    iov[0].iov_base = buf;
    iov[0].iov_len = sizeof(buf);//这里无论什么情况不要写0
    hdr.msg_iov = iov;
    hdr.msg_iovlen = 1;
    //第二个部分发送控制数据
    struct cmsghdr *pcmsghdr = (struct cmsghdr *)calloc(1,sizeof(struct cmsghdr));
    pcmsghdr->cmsg_len = CMSG_LEN(sizeof(int));//要承载的控制信息是一个整型数据
    pcmsghdr->cmsg_level = SOL_SOCKET;
    pcmsghdr->cmsg_type = SCM_RIGHTS;
    hdr.msg_control = pcmsghdr;
    hdr.msg_controllen = CMSG_LEN(sizeof(int));
    int ret = recvmsg(pipeFd,&hdr,0);
    ERROR_CHECK(ret,-1,"recvmsg");
    *pFdToRecv = *(int *)CMSG_DATA(pcmsghdr);
    return 0;
}
```
传递文件对象 固定用法，直接用，不用深究

调试senFd
```c
#include <43func.h>
int sendFd(int pipeFd, int fdToSend){
    struct msghdr hdr;
    bzero(&hdr,sizeof(hdr));
    //第一个部分发送文本数据
    struct iovec iov[1];//长度为1时，离散数据退化成连续
    char buf[10] = "hello";
    iov[0].iov_base = buf;
    iov[0].iov_len = 5;
    hdr.msg_iov = iov;
    hdr.msg_iovlen = 1;
    //第二个部分发送控制数据
    struct cmsghdr *pcmsghdr = (struct cmsghdr *)calloc(1,sizeof(struct cmsghdr));
    pcmsghdr->cmsg_len = CMSG_LEN(sizeof(int));//要承载的控制信息是一个整型数据
    pcmsghdr->cmsg_level = SOL_SOCKET;
    pcmsghdr->cmsg_type = SCM_RIGHTS;
    *(int *)CMSG_DATA(pcmsghdr) = fdToSend;
    hdr.msg_control = pcmsghdr;
    hdr.msg_controllen = CMSG_LEN(sizeof(int));
    int ret = sendmsg(pipeFd,&hdr,0);
    ERROR_CHECK(ret,-1,"sendmsg");
    return 0;
}
int recvFd(int pipeFd, int *pFdToRecv){
    struct msghdr hdr;
    bzero(&hdr,sizeof(hdr));
    //第一个部分发送文本数据
    struct iovec iov[1];//长度为1时，离散数据退化成连续
    char buf[10] = {0};
    iov[0].iov_base = buf;
    iov[0].iov_len = sizeof(buf);//这里无论什么情况不要写0
    hdr.msg_iov = iov;
    hdr.msg_iovlen = 1;
    //第二个部分发送控制数据
    struct cmsghdr *pcmsghdr = (struct cmsghdr *)calloc(1,sizeof(struct cmsghdr));
    pcmsghdr->cmsg_len = CMSG_LEN(sizeof(int));//要承载的控制信息是一个整型数据
    pcmsghdr->cmsg_level = SOL_SOCKET;
    pcmsghdr->cmsg_type = SCM_RIGHTS;
    hdr.msg_control = pcmsghdr;
    hdr.msg_controllen = CMSG_LEN(sizeof(int));
    int ret = recvmsg(pipeFd,&hdr,0);
    ERROR_CHECK(ret,-1,"recvmsg");
    *pFdToRecv = *(int *)CMSG_DATA(pcmsghdr);
    return 0;
}
int main(){
    int sv[2];
    socketpair(AF_LOCAL,SOCK_STREAM,0,sv);
    if(fork() == 0){
        int fd = open("file", O_RDWR);
        printf("child fd = %d\n", fd);
        write(fd,"hello",5);
        sendFd(sv[1],fd);
        sleep(10);
        exit(0);
    }
    else{
        int fd = open("file1",O_RDWR);
        printf("parent fd = %d\n", fd);
        int newFd;
        recvFd(sv[0],&newFd);
        printf("parent newFd = %d\n", newFd);
        write(newFd,"world",5);
        wait(NULL);
    }
}
```
发送fd和接收fd数值不一定相同
操作的是同一个文件对象

封装epoll的epollAdd( )   epollDel( )
```c
#include <43func.h>
int epollAdd(int fd, int epfd){
    struct epoll_event event;
    event.events = EPOLLIN;
    event.data.fd = fd;
    int ret = epoll_ctl(epfd,EPOLL_CTL_ADD,fd,&event);
    ERROR_CHECK(ret,-1,"epoll_ctl");
    return 0;
}
int epollDel(int fd, int epfd){
    struct epoll_event event;
    event.events = EPOLLIN;
    event.data.fd = fd;
    int ret = epoll_ctl(epfd,EPOLL_CTL_DEL,fd,&event);
    ERROR_CHECK(ret,-1,"epoll_ctl");
    return 0;
}
```

封装服务端tcp的 bind，listen
```c
// socket bind listen
#include <43func.h>
int tcpInit(int *pSockFd, char *ip, char *port){
    *pSockFd = socket(AF_INET,SOCK_STREAM,0);
    ERROR_CHECK(*pSockFd,-1,"socket");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(port));
    addr.sin_addr.s_addr = inet_addr(ip);
    int reuse = 1;
    int ret = setsockopt(*pSockFd,SOL_SOCKET,SO_REUSEADDR,&reuse,sizeof(reuse));
    ERROR_CHECK(ret,-1,"setsockopt");
    ret = bind(*pSockFd,(struct sockaddr *)&addr, sizeof(addr));
    ERROR_CHECK(ret,-1,"bind");
    listen(*pSockFd,10);
}
```

子进程的实现makeChild( )，以及子进程的事务eventHanler( )
```c
#include "head.h"
int makeChild(workerData_t *workerArr, int workerNum){
    pid_t pid;
    int pipeFd[2];
    for(int i = 0; i < workerNum; ++i){
        socketpair(AF_LOCAL,SOCK_STREAM,0,pipeFd);
        pid = fork();
        if(pid == 0){
            //子进程 确保不会脱离if结构
            close(pipeFd[0]);
            eventHanler(pipeFd[1]);
        }
        //父进程
        close(pipeFd[1]);
        printf("pipeFd = %d, pid = %d\n", pipeFd[0], pid);
        workerArr[i].pipeFd = pipeFd[0];
        workerArr[i].pid = pid;
        workerArr[i].status = FREE;
    }
}
int eventHanler(int pipeFd){
    while(1){
        int netFd;
        recvFd(pipeFd,&netFd);
        //后续的任务加在这里
        printf("I got a task!\n");
        //done
        sleep(30);
        printf("I have done this task!\n");
        pid_t pid = getpid();
        write(pipeFd,&pid,sizeof(pid));
        close(netFd);
    }
}

```

主进程 ( master process )
```c
#include <43func.h>
#include "head.h"
int main(int argc, char *argv[]){
    // ./server 192.168.14.9 1234 3
    // 创建很多子进程
    int workerNum = atoi(argv[3]);
    workerData_t *workerArr = (workerData_t *)calloc(workerNum,sizeof(workerData_t));
    makeChild(workerArr,workerNum);
    // 初始化tcp连接
    int sockFd;
    tcpInit(&sockFd,argv[1], argv[2]);
    // 用epoll把tcp连接和子进程管理起来
    int epfd = epoll_create(1);
    epollAdd(sockFd,epfd);
    for(int i = 0; i < workerNum; ++i){
        epollAdd(workerArr[i].pipeFd, epfd);
    }
    int listenSize = workerNum + 1;
    //epoll处理客户端
    struct epoll_event *readyArr = (struct epoll_event *)calloc(listenSize,sizeof(struct epoll_event));
    while(1){
        int readyNum = epoll_wait(epfd,readyArr,listenSize,-1);
        printf("epoll_wait ready!\n");
        for(int i = 0; i < readyNum; ++i){
            // epoll 处理 客户端连接
            if(readyArr[i].data.fd == sockFd){
                puts("client connect");
                int netFd = accept(sockFd,NULL,NULL);
                ERROR_CHECK(netFd,-1,"accept");
                for(int j = 0; j < workerNum; ++j){
                    if(workerArr[j].status == FREE){
                        printf("%d worker got a job, pid = %d\n", j, workerArr[j].pid);
                        sendFd(workerArr[j].pipeFd,netFd);//把网络连接移交给子进程
                        close(netFd);
                        workerArr[j].status = BUSY;
                        break;
                    }
                }
            }
            else{
                // epoll 处理 客户端终止
                puts("one worker finishes his job!\n");
                int j;
                for(j = 0; j < workerNum; ++j){
                    if(readyArr[i].data.fd == workerArr[j].pipeFd){
                        pid_t pid;
                        int ret = read(workerArr[j].pipeFd,&pid,sizeof(pid));
                        printf("%d worker finish his job, pid = %d\n", j, pid);
                        workerArr[j].status = FREE;
                        break;
                    }
                }
            }
        }
    }
}
```
文件树结构：
——client
—————client.c
——server
—————head.h
—————epollFunc.c
—————main.c
—————sendFd.c
—————tcp.c
—————worker.c

复习 进程池：
池：预先申请好所有资源，有任务到来可以使用资源，任务完成之后，可以留存资源，以供后续任务使用。
多个进程：

- 工作进程 - 开始时创建，任务完成时不销毁
- 主进程
   1. 创建子进程，管理子进程
      - 有任务到来，分配给子进程
      - 子进程完成任务，通知主进程
      - makeChild( )
         1. 创建子进程
         1. 父子进程间有一条管道
         1. 父亲拥有一个workerArr去管理所有的子进程

![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1651021389903-0cbedb7d-9f31-46ab-bfb3-3eeefd3386f6.png)

   2. 初始化tcp连接
      - 封装TCP连接 tcpInit( )
   3. 用epoll去管理多个文件描述符
      - 监听sockFd 和 每个子进程的管道   **不用监听 netFd**
      - 封装 增加监听epollAdd( ) 和 删除监听epollDel( )

开始工作：
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1651021406771-2575f237-ae6c-453c-b451-ec39349c096a.png)
`sendmsg` 和 `recvmsg`
ssize_t sendmsg(int sockfd, const struct msghdr *msg, int flags);

- int sockfd   **这里不能使用管道，限制父子进程间不能使用pipe，而应该用socketpair**
- const struct msghdr *msg
   - 控制消息msg_control 必须依附于 文本数据msg_iov(离散的数据)

例子：
`writev` - read or write data into multiple buffers   类似 sendmsg的文本传递
#include <sys/uio.h>
ssize_t readv(int fd, const struct iovec *iov, int iovcnt);
ssize_t writev(int fd, const struct iovec *iov, int iovcnt);
```c
#include <43func.h>
int main(){
    int fds[2];
    pipe(fds);
    if(fork() == 0){
        close(fds[1]);
        char buf[10] = {0};
        struct iovec iov[1];
        iov[0].iov_base = buf;
        iov[0].iov_len = sizeof(buf);
        readv(fds[0],iov,1);
        printf("I am child, buf = %s\n", buf);
        exit(0);
    }
    else{
        close(fds[0]);
        char buf[10] = "hello";
        struct iovec iov[1];
        iov[0].iov_base = buf;
        iov[0].iov_len = strlen(buf);
        writev(fds[1],iov,1);
        wait(NULL);
    }
}
```

使用sendmsg 传递 文本信息
```c
struct iovec{
    void *iov_base;		/* Starting address */
    size_t iv_len:		/* Number of bytes to transfer */
}
// iov[1]  数组长度退化为1，表示一个连续数据
```

- 发送数据的len 写 strlen( )
- 接收数据方 写 sizeof( buf )，不能写0

使用sendmsg 传递 控制信息

1. 必须使用socketpair
1. 必须包含文本信息
```c
struct cmsghdr{
    size_t cmsg_len;	/* Data byte count, including header
                           ( type is socklen_t in POSIX */
    int cmsg_level;		/* Originating protocol */
    int cmsg_type;		/* Protocol-specific type */
/* followed by
   unsiged char cmsg_data[];*/
//  最后一行说明 是 C语言中的变长数组，不能在栈上分配
};

CMSG_LEN(size_t length); //length 填 sizeof(int)
```
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1651021549830-43a67307-5546-4e19-bad1-13d22fbf49ea.png)

- 发送方：把 fd 填入 smsghdr.data
- 接收方：把 smsghdr.data 的数据 取出来，按 int 解释 文件描述符fd

总结：
主进程创建子进程：

1. 用socketpair在父子进程间建立一根管道
1. 主进程需要维护所有子进程信息
   - 前两步在 worker.c
3. 主进程初始化 tcp
   - tcp.c
4. 主进程因为epoll_wait等待
- 有新的连接  **sockeFd --accept--> netFd  --senFd-->  子进程**
- 有子进程完成工作，从管道中读到
5. 子进程启动后，陷入事件循环
- 接收任务  recvFd
- 完成任务
- 通知主进程  write管道

### 下载任务
```c
#include <43func.h>

int recvFile(int sockFd){
    //先获取文件名
    char name[1024] = {0};
    int ret = recv(sockFd,name,sizeof(name),0);
    ERROR_CHECK(ret,-1,"recv");
    int fd = open(name,O_RDWR|O_CREAT|O_TRUNC,0666);
    ERROR_CHECK(fd,-1,"open");
    char buf[4096] = {0};
    ret = recv(sockFd,buf,sizeof(buf),0);
    ERROR_CHECK(ret,-1,"recv");
    write(fd,buf,ret);
}
int main(int argc, char *argv[]){
    // ./client 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    int ret = connect(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"connect");
    recvFile(sockFd);
    close(sockFd);
}
```
```c
#include "head.h"
#include <43func.h>
int transFile(int netFd){
    int fd = open("file1", O_RDWR);
    ERROR_CHECK(fd,-1,"open");
    int ret = send(netFd,"file1",5,0);
    ERROR_CHECK(ret,-1,"send");
    char buf[4096] = {0};
    ret = read(fd, buf, sizeof(buf));
    write(netFd, buf, ret);
    close(fd);
}
```
TCP是一种字节流协议，没有消息边界
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1651022793313-bbdeef4f-ab63-4adc-aedc-b052b6d89b82.png)
“粘包问题”。
应用层私有协议：

| 长度（4B) | 有效载荷 |
| --- | --- |

```c
struct  train_s{
    int len;
    char buf[1000];
}train_t;
```

#### 小文件传输的代码
```c
#include <43func.h>
typedef struct train_s{
    int length;
    char buf[1000];
} train_t;
int recvFile(int sockFd){
    //先获取文件名
    char name[1024] = {0};
    int dataLength;
    int ret = recv(sockFd,&dataLength,sizeof(int),0);
    ERROR_CHECK(ret,-1,"recv");
    ret = recv(sockFd,name,dataLength,0);
    ERROR_CHECK(ret,-1,"recv");
    int fd = open(name,O_RDWR|O_CREAT|O_TRUNC,0666);
    ERROR_CHECK(fd,-1,"open");
    char buf[1000] = {0};
    ret = recv(sockFd,&dataLength,sizeof(int),0);
    ERROR_CHECK(ret,-1,"recv");
    ret = recv(sockFd,buf,dataLength,0);
    ERROR_CHECK(ret,-1,"recv");
    write(fd,buf,ret);
}
int main(int argc, char *argv[]){
    // ./client 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    int ret = connect(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"connect");
    recvFile(sockFd);
    close(sockFd);
}
```
```c
#include "head.h"
#include <43func.h>
int transFile(int netFd){
    int fd = open("file1", O_RDWR);
    ERROR_CHECK(fd,-1,"open");
    //int ret = send(netFd,"file1",5,0);
    //ERROR_CHECK(ret,-1,"send");
    train_t train;
    train.length = 5;
    strcpy(train.buf,"file1");
    int ret = send(netFd,&train,sizeof(train.length)+train.length,0);
    ERROR_CHECK(ret,-1,"send");
    bzero(&train,sizeof(train));
    ret = read(fd,train.buf,sizeof(train.buf));
    ERROR_CHECK(ret,-1,"read");
    train.length = ret;
    ret = send(netFd,&train,sizeof(train.length)+train.length,0);
    ERROR_CHECK(ret,-1,"send");
    close(fd);
}
```

#### 大文件传输的代码
```c
#include <43func.h>
typedef struct train_s{
    int length;
    char buf[1000];
} train_t;
int recvn(int sockFd,void *pstart,int len){
    int total = 0;
    int ret;
    char *p = (char *)pstart;
    while(total < len){
        ret = recv(sockFd,p+total,len-total,0);
        total += ret;
    }
    return 0;
}
int recvFile(int sockFd){
    //先获取文件名
    char name[1024] = {0};
    int dataLength;
    //int ret = recv(sockFd,&dataLength,sizeof(int),MSG_WAITALL);
    int ret = recvn(sockFd,&dataLength,sizeof(int));
    ERROR_CHECK(ret,-1,"recv");
    //ret = recv(sockFd,name,dataLength,MSG_WAITALL);
    ret = recvn(sockFd,name,dataLength);
    ERROR_CHECK(ret,-1,"recv");
    int fd = open(name,O_RDWR|O_CREAT|O_TRUNC,0666);
    ERROR_CHECK(fd,-1,"open");
    char buf[1000] = {0};
    while(1){
        ret = recvn(sockFd,&dataLength,sizeof(int));
        ERROR_CHECK(ret,-1,"recv");
        if(dataLength != 1000){
            printf("dataLength = %d\n", dataLength);
        }
        if(dataLength == 0){
            break;
        }
        ret = recvn(sockFd,buf,dataLength);
        ERROR_CHECK(ret,-1,"recv");
        write(fd,buf,dataLength);
    }
}
int main(int argc, char *argv[]){
    // ./client 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    int ret = connect(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"connect");
    recvFile(sockFd);
    close(sockFd);
}
```
```c
#include "head.h"
#include <43func.h>
int transFile(int netFd){
    int fd = open("file1", O_RDWR);
    ERROR_CHECK(fd,-1,"open");
    //int ret = send(netFd,"file1",5,0);
    //ERROR_CHECK(ret,-1,"send");
    train_t train;
    train.length = 5;
    strcpy(train.buf,"file1");
    int ret = send(netFd,&train,sizeof(train.length)+train.length,MSG_NOSIGNAL);
    while(1){
        bzero(&train,sizeof(train));
        ret = read(fd,train.buf,sizeof(train.buf));
        if(ret == 0){
            break;
        }
        train.length = ret;
        ret = send(netFd,&train,sizeof(train.length)+train.length,MSG_NOSIGNAL);
    }
    train.length = 0;
    ret = send(netFd,&train,sizeof(train.length)+train.length,MSG_NOSIGNAL);
    close(fd);
}
```

生成指定大小的空文件
$ truncate -M 100M filename

recv 中 len 上限是1000，不一定能读到1000
由于tcp可靠传输，未读的数据会残留。
**残留**数据的**读取方式**不正确。

错误2：

- 有一个子进程是僵尸进程
- 主进程死循环（一直就绪）

**gdb调试多进程：**
**(gdb) set follow-fork-mode child/parent**
received signal SIGPIPE, broken pipe

1. 客户端因为出错，异常中止
1. 服务端继续写，触发SIGPIPE信号
   - 服务端 临时屏蔽SIGPIPE信号   send (..., MSG_NOSIGNAL)
3. 子进程终止
- 中止的子进程 成为 僵尸进程。
- 主进程死循环 的原因：父子进程间的管道因为子进程关闭，而父进程epoll_wait一直就绪。

确保让每一次接收都能收完
方法1：
recv (..., MSG_WAITALL)
如果管道未关闭，recv确保能收到length个字节。

3GB以上大文件存在bug，自己实现类似 MSG_WAITALL的功能 ：
```c
int recvn(int sockFd, void *pstart, int len){
    int total = 0;
    int ret;
    char *p = (char *)pstart;
    while(total < len){
        ret = recv(sockFd, p + total, len - total, 0);
        total += ret;
    }
    return 0;
}
```

#### 校验不同主机上文件的内容是否一致：
摘要哈希 ：
若文件内容一致，则摘要一致。（不一致 可能有几十亿分之一的可能 一样）

| MD5 | 二者都适合，较常用 |
| --- | --- |
| SHA 1-64-228 | 碰撞几率小 |
| CRC	 | 效率高 |

$ sudo apt install openssl
$ sha1sum  filename
$ md5sum filename

### 显示进度条
客户端需要知道文件的总大小 - 服务端先发送文件的总大小
`fstat`- get file status
       #include <sys/types.h>
       #include <sys/stat.h>
       #include <unistd.h>
 int fstat(int fd, struct stat *statbuf);
```c
#include <43func.h>
typedef struct train_s{
    int length;
    char buf[1000];
} train_t;
int recvn(int sockFd,void *pstart,int len){
    int total = 0;
    int ret;
    char *p = (char *)pstart;
    while(total < len){
        ret = recv(sockFd,p+total,len-total,0);
        total += ret;
    }
    return 0;
}
int recvFile(int sockFd){
    //先获取文件名
    char name[1024] = {0};
    int dataLength;
    //int ret = recv(sockFd,&dataLength,sizeof(int),MSG_WAITALL);
    int ret = recvn(sockFd,&dataLength,sizeof(int));
    ERROR_CHECK(ret,-1,"recv");
    //ret = recv(sockFd,name,dataLength,MSG_WAITALL);
    ret = recvn(sockFd,name,dataLength);
    ERROR_CHECK(ret,-1,"recv");
    int fd = open(name,O_RDWR|O_CREAT|O_TRUNC,0666);
    ERROR_CHECK(fd,-1,"open");
    int fileSize;
    recvn(sockFd,&dataLength,sizeof(int));
    recvn(sockFd,&fileSize,dataLength);
    printf("fileSize = %d\n", fileSize);
    char buf[1000] = {0};
    int doneSize = 0;
    int lastSize = 0;
    int slice = fileSize/10000;
    while(1){
        ret = recvn(sockFd,&dataLength,sizeof(int));
        ERROR_CHECK(ret,-1,"recv");
        if(dataLength != 1000){
            printf("dataLength = %d\n", dataLength);
        }
        doneSize += dataLength;
        if(doneSize-lastSize > slice){
            printf("%5.2lf%%\r",100.0*doneSize/fileSize);
            fflush(stdout);
            lastSize = doneSize;
        }
        if(dataLength == 0){
            printf("100.00%%\n");
            break;
        }
        ret = recvn(sockFd,buf,dataLength);
        ERROR_CHECK(ret,-1,"recv");
        write(fd,buf,dataLength);
    }
}
int main(int argc, char *argv[]){
    // ./client 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    int ret = connect(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"connect");
    recvFile(sockFd);
    close(sockFd);
}
```
```c
#include "head.h"
#include <43func.h>
int transFile(int netFd){
    int fd = open("file1", O_RDWR);
    ERROR_CHECK(fd,-1,"open");
    //int ret = send(netFd,"file1",5,0);
    //ERROR_CHECK(ret,-1,"send");
    train_t train;
    train.length = 5;
    strcpy(train.buf,"file1");
    int ret = send(netFd,&train,sizeof(train.length)+train.length,MSG_NOSIGNAL);
    struct stat statbuf;
    ret = fstat(fd,&statbuf);
    ERROR_CHECK(ret,-1,"fstat");
    train.length = 4;//车厢是4个字节 int
    int fileSize = statbuf.st_size; // 长度转换成int
    memcpy(train.buf,&fileSize,sizeof(int));//int存入小火车
    send(netFd,&train,train.length+sizeof(train.length),MSG_NOSIGNAL);
    while(1){
        bzero(&train,sizeof(train));
        ret = read(fd,train.buf,sizeof(train.buf));
        if(ret == 0){
            break;
        }
        train.length = ret;
        ret = send(netFd,&train,sizeof(train.length)+train.length,MSG_NOSIGNAL);
    }
    train.length = 0;
    ret = send(netFd,&train,sizeof(train.length)+train.length,MSG_NOSIGNAL);
    close(fd);
}
```

### 零拷贝技术
mmap - 文件映射
mmap files or devices into memory
#include <sys/mman.h>
void *mmap(void *addr, size_t length, int prot, int flags, int fd, off_t offset);
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650965799690-f6e6ba02-236c-48c3-bd02-a9f83ce8fa49.png)
mmap 让 用户态空间的一部分 和 内核态的一部分 对应 同一份物理内存
```c
#include <43func.h>
typedef struct train_s{
    int length;
    char buf[1000];
} train_t;
int recvn(int sockFd,void *pstart,int len){
    int total = 0;
    int ret;
    char *p = (char *)pstart;
    while(total < len){
        ret = recv(sockFd,p+total,len-total,0);
        total += ret;
    }
    return 0;
}
int recvFile(int sockFd){
    //先获取文件名
    char name[1024] = {0};
    int dataLength;
    //int ret = recv(sockFd,&dataLength,sizeof(int),MSG_WAITALL);
    int ret = recvn(sockFd,&dataLength,sizeof(int));
    ERROR_CHECK(ret,-1,"recv");
    //ret = recv(sockFd,name,dataLength,MSG_WAITALL);
    ret = recvn(sockFd,name,dataLength);
    ERROR_CHECK(ret,-1,"recv");
    int fd = open(name,O_RDWR|O_CREAT|O_TRUNC,0666);
    ERROR_CHECK(fd,-1,"open");
    int fileSize;
    recvn(sockFd,&dataLength,sizeof(int));
    recvn(sockFd,&fileSize,dataLength);
    printf("fileSize = %d\n", fileSize);
    char buf[1000] = {0};
    time_t timeBeg,timeEnd;
    timeBeg = time(NULL);
    int doneSize = 0;
    int lastSize = 0;
    int slice = fileSize/10000;
    while(1){
        ret = recvn(sockFd,&dataLength,sizeof(int));
        ERROR_CHECK(ret,-1,"recv");
        if(dataLength != 1000){
            printf("dataLength = %d\n", dataLength);
        }
        doneSize += dataLength;
        if(doneSize-lastSize > slice){
            printf("%5.2lf%%\r",100.0*doneSize/fileSize);
            fflush(stdout);
            lastSize = doneSize;
        }
        if(dataLength == 0){
            printf("100.00%%\n");
            break;
        }
        ret = recvn(sockFd,buf,dataLength);
        ERROR_CHECK(ret,-1,"recv");
        write(fd,buf,dataLength);
    }
    timeEnd = time(NULL);
    printf("total time = %ld\n", timeEnd - timeBeg);
}
int main(int argc, char *argv[]){
    // ./client 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    int ret = connect(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"connect");
    recvFile(sockFd);
    close(sockFd);
}
```
```c
#include "head.h"
#include <43func.h>
int transFile(int netFd){
    int fd = open("file1", O_RDWR);
    ERROR_CHECK(fd,-1,"open");
    train_t train;
    train.length = 5;
    strcpy(train.buf,"file1");
    int ret = send(netFd,&train,sizeof(train.length)+train.length,MSG_NOSIGNAL);
    struct stat statbuf;
    ret = fstat(fd,&statbuf);
    ERROR_CHECK(ret,-1,"fstat");
    train.length = 4;//车厢是4个字节 int
    int fileSize = statbuf.st_size; // 长度转换成int
    memcpy(train.buf,&fileSize,sizeof(int));//int存入小火车
    send(netFd,&train,train.length+sizeof(train.length),MSG_NOSIGNAL);
    char *p = (char *)mmap(NULL,statbuf.st_size,PROT_READ|PROT_WRITE,MAP_SHARED,fd,0);
    ERROR_CHECK(p,MAP_FAILED,"mmap");
    int total = 0;
    while(total < fileSize){
        if(fileSize-total < sizeof(train.buf)){//剩余的内容不足1000字节
            train.length = fileSize-total;
        }
        else{
            train.length = sizeof(train.buf);
        }
        memcpy(train.buf,p+total,train.length);
        total += train.length;
        send(netFd,&train,train.length+sizeof(train.length),MSG_NOSIGNAL);
    }
    train.length = 0;
    ret = send(netFd,&train,sizeof(train.length)+train.length,MSG_NOSIGNAL);
    close(fd);
}
```

### 以下仅供参考：
#### 客户端也使用mmap直接一次性传递整个文件( 了解 )：

- 提前告知 文件总大小
- 依靠tcp的可靠传输
```c
send(netFd,&train,train.length+sizeof(train.length),MSG_NOSIGNAL);
char *p = (char *)mmap(NULL,fileSize,PROT_READ|PROT_WRITE,MAP_SHARED,fd,0);
ERROR_CHECK(p,MAP_FAILED,"mmap");
send(netFd, p, fileSize,MSG_NOSIGNAL);
train.length = 0;
ret = send(netFd,&train,sizeof(train.length)+train.length,MSG_NOSIGNAL);
close(fd);
munmap(p, fileSize);
```
```c
fruncate(fd, fileSize);
char *p = (char *)mmap(NULL,fileSize,PROT_READ|PROT_WRITE,MAP_SHARED,fd,0);
ERROR_CHECK(p,MAP_FAILED,"mmap");
recvn(netFd, p, fileSize);

recvn(sockFd, &dataLength,sizeof(int));
printf("dataLength = %d \n", dataLength);
close(fd);
munmap(p, fileSize);
```
工作常用**分次发**，需独立写出分次发的代码

#### 更好的接口 优化
`sendfile` - transfer data between file descriptors
       #include <sys/sendfile.h>
ssize_t sendfile(int out_fd, int in_fd, off_t *offset, size_t count);
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1651024650583-028209e3-322f-4813-8d9e-50f629be4c42.png)

- in_fd  的文件对象 必须可以mmap

sendfile 只能用于发送文件（ 服务端 ）

#### `splice` - splice data to/from a pipe  // 坑多，不常用
       #define _GNU_SOURCE         /* See feature_test_macros(7) */
       #include <fcntl.h>
ssize_t splice(int fd_in, loff_t *off_in, int fd_out,
loff_t *off_out, size_t len, unsigned int flags);

- unsigned int flags   SPLICE_F_MORE

### 进程的退出
之前：Ctrl +C   父与子进程
现在：kill -10 parent   -> kill -10 child
```c
void sigFunc(int signum){
    printf("signum = %d\n", signum);
    for(int i = 0; i < workerNum; ++i){
        kill(workerArr[i].pid,SIGUSR1);
    }
    for(int i = 0; i < workerNum; ++i){
        wait(NULL);
    }
    puts("process pool is over!");
    exit(0);
}

//父进程要注册信号
signal(SIGUSR1,sigFunc);
```

```c
#include <43func.h>
#include "head.h"
workerData_t *workerArr;
int workerNum;
void sigFunc(int signum){
    printf("signum = %d\n", signum);
    for(int i = 0; i < workerNum; ++i){
        kill(workerArr[i].pid,SIGUSR1);
    }
    for(int i = 0; i < workerNum; ++i){
        wait(NULL);
    }
    puts("process pool is over!");
    exit(0);
}
int main(int argc, char *argv[]){
    // ./server 192.168.4.19 1234 3
    // 创建很多子进程
    workerNum = atoi(argv[3]);
    //workerData_t *workerArr = (workerData_t *)calloc(workerNum,sizeof(workerData_t));
    workerArr = (workerData_t *)calloc(workerNum,sizeof(workerData_t));
    makeChild(workerArr,workerNum);
    //父进程要注册信号
    signal(SIGUSR1,sigFunc);
    // 初始化tcp连接
    int sockFd;
    tcpInit(&sockFd,argv[1], argv[2]);
    // 用epoll把tcp连接和子进程管理起来
    int epfd = epoll_create(1);
    epollAdd(sockFd,epfd);
    for(int i = 0; i < workerNum; ++i){
        epollAdd(workerArr[i].pipeFd, epfd);
    }
    int listenSize = workerNum + 1;
    struct epoll_event *readyArr = (struct epoll_event *)calloc(listenSize,sizeof(struct epoll_event));
    while(1){
        int readyNum = epoll_wait(epfd,readyArr,listenSize,-1);
        printf("epoll_wait ready!\n");
        for(int i = 0; i < readyNum; ++i){
            if(readyArr[i].data.fd == sockFd){
                puts("client connect");
                int netFd = accept(sockFd,NULL,NULL);
                ERROR_CHECK(netFd,-1,"accept");
                for(int j = 0; j < workerNum; ++j){
                    if(workerArr[j].status == FREE){
                        printf("%d worker got a job, pid = %d\n", j, workerArr[j].pid);
                        sendFd(workerArr[j].pipeFd,netFd);//把网络连接移交给子进程
                        close(netFd);
                        workerArr[j].status = BUSY;
                        break;
                    }
                }
            }
            else{
                puts("one worker finishes his job!\n");
                int j;
                for(j = 0; j < workerNum; ++j){
                    if(readyArr[i].data.fd == workerArr[j].pipeFd){
                        pid_t pid;
                        int ret = read(workerArr[j].pipeFd,&pid,sizeof(pid));
                        printf("%d worker finish his job, pid = %d\n", j, pid);
                        workerArr[j].status = FREE;
                        break;
                    }
                }
            }
        }
    }
}
```
先fork( ) 后再 signal

#### 用异步拉起同步
先有一个全局变量

1. 用epoll_监听管道读端
1. 注册SIGUSR1，在其递送时，往管道中写入

当信号产生时 -> 递送信号 -> 写管道  -> 读端就绪 -> epoll_wait就绪
```c
else if(readyArr[i].data.fd == exitPipe[0]){
                printf("process pool is going to exit!\n");
                exitFlag = 1;
                for(int j = 0; j < workerNum; ++j){
                    //kill(workerArr[j].pid,SIGUSR1);
                    sendFd(workerArr[j].pipeFd,0,exitFlag);
                }
                for(int j = 0; j < workerNum; ++j){
                    wait(NULL);
                }
                printf("process pool exits!\n");
                exit(0);
            }
```

#### 优雅退出进程池
如果子进程有任务，就不要终止，直到任务完成
方案一：用 sigpromask 屏蔽信号
方案二：在父子进程间 不使用信号

      1. 父senFd  ---> 子recvFd  只有子进程空闲时会 recvFd
      1. 发送 netFd + close
```c
int sendFd(int pipeFd, int fdToSend, int exitFlag){
    struct msghdr hdr;
    bzero(&hdr,sizeof(hdr));
    //第一个部分发送文本数据
    struct iovec iov[1];//长度为1时，离散数据退化成连续
    iov[0].iov_base = &exitFlag;
    iov[0].iov_len = sizeof(int);

    return 0;
}
```
```c
int recvFd(int pipeFd, int *pFdToRecv, int *pExitFlag){
    struct msghdr hdr;
    bzero(&hdr,sizeof(hdr));
    //第一个部分发送文本数据
    struct iovec iov[1];//长度为1时，离散数据退化成连续
    iov[0].iov_base = pExitFlag;
    iov[0].iov_len = sizeof(int );//这里无论什么情况不要写0

    return 0;
}
```
```c
int eventHanler(int pipeFd){
    while(1){
        int netFd;
        int exitFlag;
        recvFd(pipeFd,&netFd,&exitFlag);
        if(exitFlag == 1){
            puts("I am going to die!\n");
            exit(0);
        }
        //后续的任务加在这里
        printf("I got a task!\n");
        transFile(netFd);
        printf("I have done this task!\n");
        pid_t pid = getpid();
        write(pipeFd,&pid,sizeof(pid));
        close(netFd);
    }
}
```
```c
#include <43func.h>
#include "head.h"
int exitPipe[2];
void sigFunc(int signum){
    printf("signum = %d\n", signum);
    write(exitPipe[1],"1",1);
}
int main(int argc, char *argv[]){
    // ./server 192.168.4.19 1234 3
    // 创建很多子进程
    int workerNum = atoi(argv[3]);
    workerData_t * workerArr = (workerData_t *)calloc(workerNum,sizeof(workerData_t));
    makeChild(workerArr,workerNum);
    //父进程要注册信号
    signal(SIGUSR1,sigFunc);
    pipe(exitPipe);
    // 初始化tcp连接
    int sockFd;
    tcpInit(&sockFd,argv[1], argv[2]);
    // 用epoll把tcp连接和子进程管理起来
    int epfd = epoll_create(1);
    epollAdd(sockFd,epfd);
    epollAdd(exitPipe[0],epfd);
    for(int i = 0; i < workerNum; ++i){
        epollAdd(workerArr[i].pipeFd, epfd);
    }
    int listenSize = workerNum + 2;
    int exitFlag = 0;
    struct epoll_event *readyArr = (struct epoll_event *)calloc(listenSize,sizeof(struct epoll_event));
    while(1){
        int readyNum = epoll_wait(epfd,readyArr,listenSize,-1);
        printf("epoll_wait ready!\n");
        for(int i = 0; i < readyNum; ++i){
            if(readyArr[i].data.fd == sockFd){
                puts("client connect");
                int netFd = accept(sockFd,NULL,NULL);
                ERROR_CHECK(netFd,-1,"accept");
                for(int j = 0; j < workerNum; ++j){
                    if(workerArr[j].status == FREE){
                        printf("%d worker got a job, pid = %d\n", j, workerArr[j].pid);
                        sendFd(workerArr[j].pipeFd,netFd,exitFlag);//把网络连接移交给子进程
                        close(netFd);
                        workerArr[j].status = BUSY;
                        break;
                    }
                }
            } else if(readyArr[i].data.fd == exitPipe[0]){
                printf("process pool is going to exit!\n");
                exitFlag = 1;
                for(int j = 0; j < workerNum; ++j){
                    //kill(workerArr[j].pid,SIGUSR1);
                    sendFd(workerArr[j].pipeFd,0,exitFlag);
                }
                for(int j = 0; j < workerNum; ++j){
                    wait(NULL);
                }
                printf("process pool exits!\n");
                exit(0);
            }
            else{
                puts("one worker finishes his job!\n");
                int j;
                for(j = 0; j < workerNum; ++j){
                    if(readyArr[i].data.fd == workerArr[j].pipeFd){
                        pid_t pid;
                        int ret = read(workerArr[j].pipeFd,&pid,sizeof(pid));
                        printf("%d worker finish his job, pid = %d\n", j, pid);
                        workerArr[j].status = FREE;
                        break;
                    }
                }
            }
        }
    }
}
```

进程池的特点：
Nginx    Chrome(多进程)
优点：

- 一个子进程崩溃，不影响其他子进程
- 子进程数量： 1CPU - 2CPU （ **2CPU数 **应用最好）

缺点：

- 进程间通信难度大
- 消耗资源比较多

## 线程池
线程池架构
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1651030587645-f5725f23-04cc-4fcc-a6e7-1b8da2f7a2fe.png)

- 任务队列
- 共享资源 用互斥锁 保护
- 用 条件变量 实现同步
```c
typedef struct task_s{
    int netFd;//传递文件描述符
    struct task_s *pNext;
} task_t;
typedef struct taskQueue_s{
    task_t *pFront;//队首指针
    task_t *pRear;//队尾指针
    int size;//队列现在的长度
    pthread_mutex_t mutex;
    pthread_cond_t cond;
} taskQueue_t;
typedef struct threadPool_s {
    pthread_t *tid;//子线程的数组
    int threadNum;//子线程的数量
    taskQueue_t taskQueue;
    int exitFlag;
} threadPool_t;
```

### 线程池的退出
**多线程和信号不能直接混合使用**
不知道递送信号的线程是哪一个
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1651044532372-a57f04d6-06b9-4934-80b7-dfd77b190121.png)

1. 先创建一根管道
1. 父进程  --frok( )--> 子进程
1. 父进程负责注册SIGUSR1信号，递送信号时，写入管道
1. 读到管道，子进程里主线程的epoll_wait 就绪
   1. pthread_cancel
   1. pthrread_join
   1. pthread_exit

**可以 确保接收信号是子进程的主线程。**

线程终止的时候出现了死锁
```c
void sigFunc(int signum){
    printf("signum = %d\n", signum);
    write(exitPipe[1],"1",1);
    printf("Parent process is going to die!\n");
}
int main(int argc, char *argv[]){
    //./server 192.168.14.9 1234 3
    ARGS_CHECK(argc,4);
    pipe(exitPipe);
    if(fork() != 0){//父进程 先注册一个信号，等待子进程终止，最后自己终止
        close(exitPipe[0]);
        signal(SIGUSR1,sigFunc);
        wait(NULL);
        exit(0);
    }
    close(exitPipe[1]);
    ...
    ...
    epollAdd(exitPipe[0],epfd);
```
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1651201282692-2b2bb246-fcbc-44b2-843b-2dacc238356b.png)
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1651201293726-13338747-0704-4279-b8d4-a8a7419f68ce.png)
资源清理的方法：

1. 在 lock 之后 pthread_cleanup_push( )
1. 在unlock的位置 替换成 pthread_cleanup_pop( )
```c
void cleanFunc(void *arg){
    threadPool_t * pThreadPool = (threadPool_t *)arg;
    pthread_mutex_unlock(&pThreadPool->taskQueue.mutex);
}
void * handleEvent(void *arg){
    threadPool_t * pThreadPool = (threadPool_t *)arg;
    int netFd;
    while(1){
        printf("I am free! tid = %lu\n", pthread_self());
        pthread_mutex_lock(&pThreadPool->taskQueue.mutex);
        pthread_cleanup_push(cleanFunc,(void *)pThreadPool);
        while(pThreadPool->taskQueue.size == 0 && pThreadPool->exitFlag == 0){
            pthread_cond_wait(&pThreadPool->taskQueue.cond,&pThreadPool->taskQueue.mutex);
        }

        //现在子线程苏醒了
        netFd = pThreadPool->taskQueue.pFront->netFd;//拿到了队首的文件描述符
        taskDequeue(&pThreadPool->taskQueue);
        pthread_cleanup_pop(1);

        printf("I am working! tid = %lu\n", pthread_self());
        transFile(netFd);
        printf("done\n");
        close(netFd);
    }
}
```

优雅退出（ 不使用pthread_cancel )
设置一个 共享的 exit_flag ;

1. flag = 0 ；
1. 主线程在管道exitPipe就绪  flag = 1 ；
1. 子线程在接收任务前检查（ flag == 1），则终止。

客户端：
```c
#include <43func.h>
typedef struct train_s{
    int length;
    char buf[1000];
} train_t;
int recvn(int sockFd,void *pstart,int len){
    int total = 0;
    int ret;
    char *p = (char *)pstart;
    while(total < len){
        ret = recv(sockFd,p+total,len-total,0);
        total += ret;
    }
    return 0;
}
int recvFile(int sockFd){
    //先获取文件名
    char name[1024] = {0};
    int dataLength;
    //int ret = recv(sockFd,&dataLength,sizeof(int),MSG_WAITALL);
    int ret = recvn(sockFd,&dataLength,sizeof(int));
    ERROR_CHECK(ret,-1,"recv");
    //ret = recv(sockFd,name,dataLength,MSG_WAITALL);
    ret = recvn(sockFd,name,dataLength);
    ERROR_CHECK(ret,-1,"recv");
    int fd = open(name,O_RDWR|O_CREAT|O_TRUNC,0666);
    ERROR_CHECK(fd,-1,"open");
    int fileSize;
    recvn(sockFd,&dataLength,sizeof(int));
    recvn(sockFd,&fileSize,dataLength);
    printf("fileSize = %d\n", fileSize);
    char buf[1000] = {0};
    time_t timeBeg,timeEnd;
    timeBeg = time(NULL);

    int pipefds[2];
    pipe(pipefds);
    int total = 0;
    while(total < fileSize){
        int ret = splice(sockFd,NULL,pipefds[1],NULL,4096,SPLICE_F_MORE);
        total += ret;
        //usleep(2000000);
        splice(pipefds[0],NULL,fd,NULL,ret,SPLICE_F_MORE);
    }
    /*
    ftruncate(fd,fileSize);
    char *p = (char *)mmap(NULL,fileSize,PROT_READ|PROT_WRITE,MAP_SHARED,fd,0);
    ERROR_CHECK(p,MAP_FAILED,"mmap");
    recvn(sockFd,p,fileSize);
    */
    recvn(sockFd,&dataLength,sizeof(int));
    printf("dataLength = %d\n", dataLength);
    timeEnd = time(NULL);
    printf("total time = %ld\n", timeEnd - timeBeg);
}
int main(int argc, char *argv[]){
    // ./client 192.168.14.9 1234
    ARGS_CHECK(argc,3);
    int sockFd = socket(AF_INET,SOCK_STREAM,0);
    ERROR_CHECK(sockFd,-1,"socket");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(argv[2]));
    addr.sin_addr.s_addr = inet_addr(argv[1]);
    int ret = connect(sockFd,(struct sockaddr *)&addr,sizeof(addr));
    ERROR_CHECK(ret,-1,"connect");
    recvFile(sockFd);
    close(sockFd);
}
```
```c
SRCS:=$(wildcard *.c)
EXES:=$(patsubst %.c,%,$(SRCS))
all:$(EXES)
%:%.c
	gcc $^ -o $@ -g -pthread
clean:
	$(RM) $(EXES)
```

服务端:
```c
SRCS:= $(wildcard *.c)
OBJS:= $(patsubst %.c,%.o,$(SRCS))
CC:= gcc
server: $(OBJS)
	$(CC) $^ -o $@ -pthread -g
%.o:%.c
	$(CC) -c $^ -o $@ -g
clean:
	$(RM) $(OBJS) server
rebuild:clean server
```
```c
#include <43func.h>
typedef struct task_s{
    int netFd;//传递文件描述符
    struct task_s *pNext;
} task_t;
typedef struct taskQueue_s{
    task_t *pFront;//队首指针
    task_t *pRear;//队尾指针
    int size;//队列现在的长度
    pthread_mutex_t mutex;
    pthread_cond_t cond;
} taskQueue_t;
typedef struct threadPool_s {
    pthread_t *tid;//子线程的数组
    int threadNum;//子线程的数量
    taskQueue_t taskQueue;
    int exitFlag;
} threadPool_t;
int taskEnqueue(taskQueue_t *pTaskQueue, int netFd);
int taskDequeue(taskQueue_t *pTaskQueue);
int threadPoolInit(threadPool_t *pThreadPool, int workerNum);
int makeWorker(threadPool_t *pThreadPool);
int tcpInit(int *pSockFd, char *ip, char *port);
int epollAdd(int fd, int epfd);
int epollDel(int fd, int epfd);
int transFile(int netFd);
typedef struct train_s{
    int length;
    char buf[1000];
} train_t;
```
```c
#include <43func.h>
int epollAdd(int fd, int epfd){
    struct epoll_event event;
    event.events = EPOLLIN;
    event.data.fd = fd;
    int ret = epoll_ctl(epfd,EPOLL_CTL_ADD,fd,&event);
    ERROR_CHECK(ret,-1,"epoll_ctl");
    return 0;
}
int epollDel(int fd, int epfd){
    struct epoll_event event;
    event.events = EPOLLIN;
    event.data.fd = fd;
    int ret = epoll_ctl(epfd,EPOLL_CTL_DEL,fd,&event);
    ERROR_CHECK(ret,-1,"epoll_ctl");
    return 0;
}
```
```c
#include "threadPool.h"
int taskEnqueue(taskQueue_t *pTaskQueue, int netFd){
    task_t *pTask = (task_t *)calloc(1,sizeof(task_t));
    pTask->netFd = netFd;
    if(pTaskQueue->size == 0){
        pTaskQueue->pFront = pTask;
        pTaskQueue->pRear = pTask;
    }
    else{
        pTaskQueue->pRear->pNext = pTask;
        pTaskQueue->pRear = pTask;
    }
    ++pTaskQueue->size;
    return 0;
}
int taskDequeue(taskQueue_t *pTaskQueue){
    task_t *pCur = pTaskQueue->pFront;
    pTaskQueue->pFront = pCur->pNext;
    free(pCur);
    --pTaskQueue->size;
    return 0;
}
```
```c
// socket bind listen
#include <43func.h>
int tcpInit(int *pSockFd, char *ip, char *port){
    *pSockFd = socket(AF_INET,SOCK_STREAM,0);
    ERROR_CHECK(*pSockFd,-1,"socket");
    struct sockaddr_in addr;
    addr.sin_family = AF_INET;
    addr.sin_port = htons(atoi(port));
    addr.sin_addr.s_addr = inet_addr(ip);
    int reuse = 1;
    int ret = setsockopt(*pSockFd,SOL_SOCKET,SO_REUSEADDR,&reuse,sizeof(reuse));
    ERROR_CHECK(ret,-1,"setsockopt");
    ret = bind(*pSockFd,(struct sockaddr *)&addr, sizeof(addr));
    ERROR_CHECK(ret,-1,"bind");
    listen(*pSockFd,10);
}
```
```c
#include "threadPool.h"
#include <43func.h>
int transFile(int netFd){
    int fd = open("file1", O_RDWR);
    ERROR_CHECK(fd,-1,"open");
    //int ret = send(netFd,"file1",5,0);
    //ERROR_CHECK(ret,-1,"send");
    train_t train;
    train.length = 5;
    strcpy(train.buf,"file1");
    int ret = send(netFd,&train,sizeof(train.length)+train.length,MSG_NOSIGNAL);
    struct stat statbuf;
    ret = fstat(fd,&statbuf);
    ERROR_CHECK(ret,-1,"fstat");
    train.length = 4;//车厢是4个字节 int
    int fileSize = statbuf.st_size; // 长度转换成int
    memcpy(train.buf,&fileSize,sizeof(int));//int存入小火车
    send(netFd,&train,train.length+sizeof(train.length),MSG_NOSIGNAL);
    /*
    char *p = (char *)mmap(NULL,fileSize,PROT_READ|PROT_WRITE,MAP_SHARED,fd,0);
    ERROR_CHECK(p,MAP_FAILED,"mmap");
    send(netFd,p,fileSize,MSG_NOSIGNAL);
    */
    sendfile(netFd,fd,NULL,fileSize);
    train.length = 0;
    ret = send(netFd,&train,sizeof(train.length)+train.length,MSG_NOSIGNAL);
    close(fd);
}
```
```c
#include "threadPool.h"
void cleanFunc(void *arg){
    threadPool_t * pThreadPool = (threadPool_t *)arg;
    pthread_mutex_unlock(&pThreadPool->taskQueue.mutex);
}
void * handleEvent(void *arg){
    threadPool_t * pThreadPool = (threadPool_t *)arg;
    int netFd;
    while(1){
        printf("I am free! tid = %lu\n", pthread_self());
        pthread_mutex_lock(&pThreadPool->taskQueue.mutex);
        pthread_cleanup_push(cleanFunc,(void *)pThreadPool);
        while(pThreadPool->taskQueue.size == 0 && pThreadPool->exitFlag == 0){
            pthread_cond_wait(&pThreadPool->taskQueue.cond,&pThreadPool->taskQueue.mutex);
        }
        if(pThreadPool->exitFlag != 0){
            printf("I am going to die child thread!\n");
            pthread_exit(NULL);
        }
        //现在子线程苏醒了
        netFd = pThreadPool->taskQueue.pFront->netFd;//拿到了队首的文件描述符
        taskDequeue(&pThreadPool->taskQueue);
        pthread_cleanup_pop(1);
        printf("I am working! tid = %lu\n", pthread_self());
        transFile(netFd);
        printf("done\n");
        close(netFd);
    }
}
int makeWorker(threadPool_t *pThreadPool){
    for(int i = 0; i < pThreadPool->threadNum; ++i){
        pthread_create(&pThreadPool->tid[i],NULL,handleEvent,(void *)pThreadPool);
    }
}
```
```c
#include <43func.h>
#include "threadPool.h"
int exitPipe[2];
void sigFunc(int signum){
    printf("signum = %d\n", signum);
    write(exitPipe[1],"1",1);
    printf("Parent process is going to die!\n");
}
int main(int argc, char *argv[]){
    //./server 192.168.14.9 1234 3
    ARGS_CHECK(argc,4);
    pipe(exitPipe);
    if(fork() != 0){//父进程 先注册一个信号，等待子进程终止，最后自己终止
        close(exitPipe[0]);
        signal(SIGUSR1,sigFunc);
        wait(NULL);
        exit(0);
    }
    close(exitPipe[1]);
    int workerNum = atoi(argv[3]);
    threadPool_t threadPool;//为线程池的任务队列、子线程的tid申请内存
    threadPoolInit(&threadPool,workerNum);//初始化内存
    makeWorker(&threadPool);//创建若干个子线程
    int sockFd;
    tcpInit(&sockFd,argv[1],argv[2]);//主线程要初始化TCP连接
    int epfd = epoll_create(1);
    epollAdd(sockFd,epfd);//用epoll把sockfd监听起来
    epollAdd(exitPipe[0],epfd);
    struct epoll_event readyArr[2];
    while(1){
        int readyNum = epoll_wait(epfd,readyArr,2,-1);
        printf("epoll_wait returns\n");
        for(int i = 0; i < readyNum; ++i){
            if(readyArr[i].data.fd == sockFd){
                //说明客户端有新的连接到来
                int netFd = accept(sockFd,NULL,NULL);
                //先加锁
                pthread_mutex_lock(&threadPool.taskQueue.mutex);
                taskEnqueue(&threadPool.taskQueue,netFd);//生产一个任务
                printf("New task!\n");
                pthread_cond_signal(&threadPool.taskQueue.cond);//通知处于唤醒队列的线程
                pthread_mutex_unlock(&threadPool.taskQueue.mutex);
            }
            else if(readyArr[i].data.fd == exitPipe[0]){
                printf("child process, threadPool is going to die\n");
                //for(int j = 0;j < workerNum; ++j){
                //    pthread_cancel(threadPool.tid[j]);
                //}
                threadPool.exitFlag = 1;
                pthread_cond_broadcast(&threadPool.taskQueue.cond);
                for(int j = 0;j < workerNum; ++j){
                    pthread_join(threadPool.tid[j],NULL);
                }
                pthread_exit(NULL);
            }
        }
    }

}
int threadPoolInit(threadPool_t *pThreadPool, int workerNum){
    pThreadPool->threadNum = workerNum;
    pThreadPool->tid = (pthread_t *)calloc(workerNum,sizeof(pthread_t));
    pThreadPool->taskQueue.pFront = NULL;
    pThreadPool->taskQueue.pRear = NULL;
    pThreadPool->taskQueue.size = 0;
    pthread_mutex_init(&pThreadPool->taskQueue.mutex,NULL);
    pthread_cond_init(&pThreadPool->taskQueue.cond,NULL);
    pThreadPool->exitFlag = 0;
}
```
