进程切换： PC 、 stack

- CPU寄存器要切换 （上下文切换）
- 虚拟地址和物理地址的映射要置换 （页表切换）

进程间通信比较麻烦。

线程（轻量级进程） 是一个正在执行的程序。
同一个进程存在多个线程，多个线程共享内存资源。
线程是CPU**调度**的基本单位。
多线程之间是并发的。

$ man 7 pthread
NPTL  	用户级进程：内核级进程 = 1:1

- 用户级线程：调度由进程处理
- 内核级线程：调度由操作系统内核处理

**CPU调度**以**线程**为单位，之前的**进程**可看作**单线程进程.**
linux的实现中，每个线程都有自己的一个task_struct （CPU资源调度）
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650091541868-ec67d6d2-2d9d-4e0e-82ef-cec0405f1e22.png)
线程切换 相较于 进程切换 的优点：

- 减少了上下文切换的代价。
- 不需要页表切换。
- 线程是共享内存，通信简单。

多线程中**不能使用**`**perror**`
全局变量errno （ 可以通过 #include <errno.h>查看 ) 存在于数据段中。
多线程用返回值的数值（存于栈中） 来处理报错。
char *strerror(int errnum);

验证 之前的进程都是单线程进程。
进入 /proc/ 查看每个进程的 task directory 是否有多个线程。

### 线程基础接口
`pthread_create` - create a new thread
一启动进程，就创建了主线程 ( 栈区从main开始压栈）
#include <pthread.h>
int pthread_create(pthread_t *thread, const pthread_attr_t *attr, void *(*start_routine) (void *), void *arg);

- pthread_t *thread  // 线程ID
- const pthread_attr_t *attr  // 线程属性 ( 默认属性 NULL )
- void *(*start_routine) (void *) // 线程启动函数（子线程的main）
- void *arg		// 传递给start_routine 的参数

Compile and link with** -pthread.  链接时加上选项 **

一个进程能创建多少个线程
```c
#include <43func.h>
void * threadFunc(void *arg){
    //printf("I am child thread, tid = %lu\n", pthread_self());
    while(1){
        ;
    }
}
int main(){
    pthread_t tid;
    for(int i = 0;;++i){
        int ret = pthread_create(&tid,NULL,threadFunc,NULL);
        THREAD_ERROR_CHECK(ret,"pthread_create");
        if(ret != 0){
            printf("i = %d\n", i);
            break;
        }
    }
}
// pthread_creat:resource temporaily unavailable
// i = 4924
// 128TB 2^47    //x86-64物理地址256TB  2^48  1:1
```

`pthread_self`- obtain ID of the calling thread
#include <pthread.h>
pthread_t pthread_self(void);
       Compile and link with -pthread.

```c
#include <43func.h>
void *threadFunc(void *arg){
    printf("I am child thread, tid = %lu\n", pthread_self());
}
int main(){
    printf("I am main thread, tid = %lu\n", pthread_self());
    pthread_t tid;
    pthread_create(&tid,NULL,threadFunc,NULL);
    //sleep(1);
    usleep(20);
    //return 0;
    _exit(0);
}
//
// I am child thread, tid =
// I am main thread, tid =
// I am main thread, tid =
//
// I am child thread, tid =
// I am main thread, tid =
```
输出两次/三次的问题
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650095020345-71833235-922a-4518-bc9d-c7a108a60ac0.png)

1. 调用printf, 数据拷贝到`stdout`
1. `stdout` 拷贝到 struct file
1. 将`stdout`清空
- 若在 1-2之间 return 0； 会显示一次child
- 若2-3之间 或 3之后 return 0； 会显示两次child

`**printf( )**` 实现了自己的**原子**操作

### 线程之间拷贝与共享数据
多线程共享内存地址空间
```c
#include <43func.h>
int global = 10;
void * threadFunc(void *arg){
    printf("I am child thread, tid = %lu\n", pthread_self());
    printf("child thread, global = %d\n",global);
}
int main(){
    printf("I am main thread, tid = %lu\n", pthread_self());
    pthread_t tid;
    int ret = pthread_create(&tid,NULL,threadFunc,NULL);
    THREAD_ERROR_CHECK(ret,"pthread_create");
    sleep(1);
    global = 200;
    printf("main thread, global = %d\n",global);
    sleep(1);
}
// I am main thread, tid = 140572290385728
// I am child thread, tid = 140572290320128
// child thread, global = 10 // sleep(1)
// main thread, global = 200
```

多线程共享堆空间
主线程和子线程使用同一个数值的地址。
pthread_create(&tid,NULL,threadFunc,(void *)pHeap);
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650285788785-a6710415-91ad-4aaf-9bb2-84c5e12ab4a7.png)
```c
#include <43func.h>
void *threadFunc(void *arg)
{
    printf("I am child thread, tid = %lu\n", pthread_self());
    int *pHeap = (int *)arg;
    *pHeap = 100;
    printf("child thread, *pHead = %d\n", *pHeap);
}
int main()
{
    printf("I am main thread, tid = %lu\n", pthread_self());
    int *pHeap = (int *)malloc(sizeof(int));
    *pHeap = 0;
    pthread_t tid;
    int ret = pthread_create(&tid, NULL, threadFunc, (void *)pHeap);
    THREAD_ERROR_CHECK(ret, "pthread_create");
    sleep(1);
    printf("main thread, *pHead = %d\n", *pHeap);
}
// I am main thread, tid = 140180802701120
// I am child thread, tid = 140180802635520
// child thread, *pHead = 100
// main thread, *pHead = 100
```

多线程之间传递一个整数  ( **拷贝** )
直接共享一个long类型，即把 void * 当成一个long 使用
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650285843850-7c9f75fd-1c8c-4708-90af-f561233f8a46.png)
```c
#include <43func.h>
void * threadFunc(void *arg){
    long i = (long) arg;
    printf("I am child thread, tid = %lu, i = %ld\n", pthread_self(), i);
}
int main(){
    pthread_t tid;
    long i = 1;
    int ret = pthread_create(&tid,NULL,threadFunc,(void *)i);
    THREAD_ERROR_CHECK(ret,"pthread_create");
    i = 2;
    ret = pthread_create(&tid,NULL,threadFunc,(void *)i);
    THREAD_ERROR_CHECK(ret,"pthread_create");
    printf("I am main thread, tid = %lu, i = %ld\n", pthread_self(), i);
    sleep(2);
}
// I am child thread, tid = 139876068427520, i = 1
// I am main thread, tid = 139876068493120, i = 2
// I am child thread, tid = 139876059973376, i = 2
```

多线程共享一个整数 ( 指针 )
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650285860671-3f2e91e9-86a9-4494-81f6-75aa54813fce.png)
```c
#include <43func.h>
void *threadFunc(void *arg)
{
    long *pi = (long *)arg;
    printf("I am child thread, tid = %lu, i = %ld\n", pthread_self(), *pi);
}
int main()
{
    pthread_t tid;
    long i = 1;
    int ret = pthread_create(&tid, NULL, threadFunc, (void *)&i);
    THREAD_ERROR_CHECK(ret, "pthread_create");
    i = 2;
    ret = pthread_create(&tid, NULL, threadFunc, (void *)&i);
    THREAD_ERROR_CHECK(ret, "pthread_create");
    sleep(2);
}
// I am child thread, tid = 140357414815488, i = 2
// I am child thread, tid = 140357406361344, i = 2
```

( void * ) args 的两种用法 :

- 想要传递 用long 	**拷贝**
- 想要共享 用指针	**共享**

二者都可以转成 void *

多线程的的栈区是相对独立的：
一个线程可以通过地址去访问另一个线程的栈区。

### 线程的中止
一个进程中的任意一个线程

- main中return
- exit
- _exit/ _Exit
- abort
- 收到信号

整个进程终止，其中所有的线程**都会终止**。

子线程如何只终止自己

- 从 threadFunc中return NULL  //尽量不要用
- 从任何位置调用 `pthread_exit`( )

`pthread_exit`( void *retval)  	// void *retval  子线程的返回值 (void *)

`pthread_join` - join with a terminated thread //回收线程的资源
#include <pthread.h>
int pthread_join(pthread_t thread, void **retval);
       Compile and link with -pthread.

- pthread_t thread 	// 目标线程的tid 不是指针
- void **retval		//

![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650252813265-8b8abd75-fe9e-4a56-84c1-2061f418722e.png)

      1. 拷贝子线程的终止状态
      1. 主调函数申请了(void *)的一个内存
      1. join 试图修改 主调函数中(void *)指向的内容
         - 需要使用地址，因此使用 ( void  ** )

类似于多进程的wait  **等待任何一个线程的终止**。
```c
#include <43func.h>
void * threadFunc(void *arg){
    printf("I am child thread, tid = %lu\n", pthread_self());
    //return (void *)12345;
    pthread_exit((void *)23456);
}
int main(){
    printf("I am main thread, tid = %lu\n", pthread_self());
    pthread_t tid;
    int ret = pthread_create(&tid,NULL,threadFunc,NULL);
    THREAD_ERROR_CHECK(ret,"pthread_create");

    //long retval;//申请了8个字节
    //pthread_join(tid,(void **)&retval);
    //主线程要拿&retval所指的内存空间，存储子线程的返回值
    //printf("retval = %ld\n", retval);

    //void *retval;//申请了8个字节
    //pthread_join(tid,&retval);
    //printf("retval = %ld\n", (long) retval);

    //ret = pthread_join(&tid,NULL);//错误的用法
    //ret = pthread_join(NULL,NULL);//错误的用法
    ret = pthread_join(tid,NULL);//正确的用法

    THREAD_ERROR_CHECK(ret,"pthread_join");
}
```

多线程程序不能使用信号
多个线程都会收到注册信号的信息 ，用户无法得知是**哪个线程递送信号**

线程的**取消**类似于**信号**
pthread_cancel - send a cancellation request to a thread
#include <pthread.h>
int pthread_cancel(pthread_t thread);
       Compile and link with -pthread.

1. 给另一个进程发送取消请求
1. 将取消标志置为1
1. “特殊函数”（ **取消点** ），中止线程

操作文件
可能发生阻塞

$ man 7 threads    	// 查看取消点
pthread_mutex_lock		//  加锁，不是取消点
```c
accept()
close()
connect()
open()
pthread_cond_timedwait()
pthread_cond_wait()
pthread_join()
pthread_testcancel()
read()

printf( ) 会运行到printf执行完之后
```

```c
#include <43func.h>
void * threadFunc(void *arg){
    printf("I am child thread, tid = %lu\n", pthread_self());
    pthread_exit((void *)0);
}
int main(){
    printf("I am main thread, tid = %lu\n", pthread_self());
    pthread_t tid;
    int ret = pthread_create(&tid,NULL,threadFunc,NULL);
    THREAD_ERROR_CHECK(ret,"pthread_create");
    pthread_cancel(tid);
    void * retval;
    pthread_join(tid,&retval);//如果tid是被cancel掉的，retval就是-1
    printf("child thread return value = %ld\n", (long)retval);
}
```
如果tid是被cancel的，retval就是-1

```c
#include <43func.h>
void * threadFunc(void *arg){
    //printf("I am child thread, tid = %lu\n", pthread_self());
    while(1){}
    pthread_exit((void *)0);
}
int main(){
    printf("I am main thread, tid = %lu\n", pthread_self());
    pthread_t tid;
    int ret = pthread_create(&tid,NULL,threadFunc,NULL);
    THREAD_ERROR_CHECK(ret,"pthread_create");
    pthread_cancel(tid);
    void * retval;
    pthread_join(tid,&retval);//如果tid是被cancel掉的，retval就是-1
    printf("child thread return value = %ld\n", (long)retval);
}
```
$ ps -elLf

`pthread_testcancel` - request delivery of any pending cancellation request
#include <pthread.h>
void pthread_testcancel(void);
       Compile and link with -pthread.
如果取消标志位为真，终止本线程。
```c
#include <43func.h>
void * threadFunc(void *arg){
    //printf("I am child thread, tid = %lu\n", pthread_self());
    while(1){
        pthread_testcancel();// 取消点 函数
    }
    pthread_exit((void *)0);
}
int main(){
    printf("I am main thread, tid = %lu\n", pthread_self());
    pthread_t tid;
    int ret = pthread_create(&tid,NULL,threadFunc,NULL);
    THREAD_ERROR_CHECK(ret,"pthread_create");
    pthread_cancel(tid);
    void * retval;
    pthread_join(tid,&retval);//如果tid是被cancel掉的，retval就是-1
    printf("child thread return value = %ld\n", (long)retval);
}
```
cancel不是立刻终止一个线程：

1. cancel会立刻修改目标线程的取消标志位
1. 目标线程运行到一些特殊的函数  （ 取消点 ）
1. 取消点函数调用完成**前后**会终止线程
   - malloc 等前
   - printf 后

![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650265577766-07722c8e-5c51-491c-bcb0-f239d545e293.png)
异步终止可能引发资源泄漏。
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650290211377-04089ed0-6676-4bb6-9d91-b5995f562bae.png)

1. 目标线程运行到一些特殊的函数  （ 取消点 ）
1. 取消点函数调用完前会终止线程
1. 运行线程取消清理函数
1. 取消点函数调用完前会终止线程

资源清理栈 自动根据申请的资源数量释放
`pthread_cleanup_push`  - push thread cancellation clean-up handlers
`pthread_cleanup_pop` - pop thread cancellation clean-up handlers
 #include <pthread.h>
void pthread_cleanup_push(void (*routine)(void *),  void *arg);
void pthread_cleanup_pop(int execute);
       Compile and link with -pthread.
申请资源 	   对应 		释放行为
malloc					free
open/fopen/opendir		close/fclose/closedir
semop/mutex_lock			semop/mutex_unlock

申请资源之后，把对应的释放行为压栈( pthread_cleanup_push)
当线程因为pthread_exit 或 cancel 终止时，将栈清空（不包括启动函数中return )
线程可以主动调用pthread_cleanup_pop 释放资源。

- 线程因为取消而终止时，所有清理函数按照后进先出的顺序从栈中弹出并执行。
- 线程调用 pthread_exit 而主动终止时，所有清理函数按照后进先出的顺序从栈中弹出并执行。
- 线程调用 pthread_clean_pop 并且 execute 参数非0时，弹出栈顶的清理函数并执行。
- 线程调用 pthread_clean_pop 并且 execute 参数为0时，弹出栈顶的清理函数不执行。

封装资源清理行为
int close (int fd);  	->
void cleanup(void *args){
int *p = (int *)p;
close(*p);
}
```c
#include <43func.h>
void clean1(void *arg){
    printf("I am clean1\n");
    free(arg);
}
void clean2(void *arg){
    printf("I am clean2\n");
    free(arg);
}
void * threadFunc(void *arg){
    printf("I am child thread, tid = %lu\n", pthread_self());
    int *p1 = malloc(4);//申请资源后马上将对应的清理行为压栈
    pthread_cleanup_push(clean1,p1);
    int *p2 = malloc(4);
    pthread_cleanup_push(clean2,p2);
    //return NULL;
    //pthread_cleanup_pop(1);// 主动调用清理函数
    //pthread_cleanup_pop(1);// 主动调用清理函数
    //sleep(1);
    int i;
    pthread_cleanup_pop(0); // 不会主动调用清理函数
    pthread_cleanup_pop(0); // 不会主动调用清理函数
    //i = 1;
    pthread_exit(NULL);
}
int main(){
    printf("I am main thread, tid = %lu\n", pthread_self());
    pthread_t tid;
    int ret = pthread_create(&tid,NULL,threadFunc,NULL);
    THREAD_ERROR_CHECK(ret,"pthread_create");
    pthread_join(tid,NULL);
}
```

- `pthread_exit( )` 和 `pthread_ancel( )`导致终止，会调用清理函数
- 在线程入口函数return 不会调用清理函数

一律使用`**pthread_exit( )**`** 终止**。

pthread_cleanup_pop( )

- 正数		主动调用清理函数
-    0 		不会主动调用清理函数

linux规定：push 和 pop 必须**成对出现**，必须在**同一个作用域中成对出现**
**定义变量 需要在 push和pop外**
**“谁加锁谁解锁”**

资源清理总结：
cleanup的目的：不论线程何时终止，都会根据申请资源执行合理的释放行为。
核心数据结构：清理函数栈
用法：

1. 每次申请资源后，push( cleanup)
1. 弹栈时机
   - pthread_exit( )  pthread_cancel( ) 会运行到取消点，清理  LIFO
   - pthread_cleanup_pop  弹栈一次 （参数为正）
   - 不要使用return
3. push和pop成对出现
3. 有了pop，不要使用free( )手动回收

### 竞争 与 互斥( mutually exclusion)
mutex 互斥锁
一个线程可以做加锁操作

- 若已有任何线程持有锁，则准备加锁的线程会阻塞  	P
- 若未加锁，加锁操作将加锁，线程继续运行			V

```c
// 前三个为静态初始化
 pthread_mutex_t fastmutex = PTHREAD_COND_INITIALIZER;
 pthread_mutex_t recmutex = PTHREAD_RECURSIVE_MUTEX_INITIALIZER_NP;
 pthread_mutex_t errchkmutex=PTHREAD_ERRORCHECK_MUTEX_INITIALIZER_NP;

int pthread_mutex_init(pthread_mutex_t *mutex, const pthread_mutexattr_t *mutexattr);
int pthread_mutex_destroy(pthread_mutex_t *mutex);
int pthread_mutex_lock(pthread_mutex_t *mutex); // 加锁
int pthread_mutex_unlock(pthread_mutex_t *mutex); // 解锁
int pthread_mutex_trylock(pthread_mutex_t *mutex);  // 销毁锁
```

共享资源最好封装 （ mutex是共享资源）
```c
typedef struct shareRes_s{
    pthread_mutex_t mutex;
    int count;
} shareRes_t;
```

```c
#include <43func.h>
#define NUM 10000000
typedef struct shareRes_s{
    pthread_mutex_t mutex;
    int count;
} shareRes_t;
void * threadFunc(void *arg){
    shareRes_t * pShareRes = (shareRes_t *)arg;
    for(int i = 0; i < NUM; ++i){
        pthread_mutex_lock(&pShareRes->mutex);
        ++pShareRes->count;
        pthread_mutex_unlock(&pShareRes->mutex);
    }
}
int main(){
    //pthread_mutex_t mutex = PTHREAD_MUTEX_INITIALIZER;//静态初始化

    //pthread_mutex_t mutex;
    //pthread_mutex_init(&mutex,NULL);//如果第二个参数是NULL，表示以默认形式初始化锁
    //pthread_mutex_destroy(&mutex);

    shareRes_t shareRes;
    shareRes.count = 0;
    pthread_mutex_init(&shareRes.mutex,NULL);
    pthread_t tid;
    struct timeval timeBeg,timeEnd;
    gettimeofday(&timeBeg,NULL);
    int ret = pthread_create(&tid,NULL,threadFunc,(void *)&shareRes);
    THREAD_ERROR_CHECK(ret,"pthread_create");
    for(int i = 0; i < NUM; ++i){
        pthread_mutex_lock(&shareRes.mutex);
        ++shareRes.count;
        pthread_mutex_unlock(&shareRes.mutex);
    }
    pthread_join(tid,NULL);
    gettimeofday(&timeEnd,NULL);
    printf("total time = %ld us\n",
     1000000*(timeEnd.tv_sec - timeBeg.tv_sec) + timeEnd.tv_usec - timeBeg.tv_usec);
    printf("count = %d\n", shareRes.count);
    pthread_mutex_destroy(&shareRes.mutex);
}

```
使用互斥锁
单次操作 	0.01us    10ns
注意事项：

- 共享资源最好封装 （ mutex是共享资源）
- usertype   ->  void *   ->  usertype
- 所有的共享资源的访问要放入临界区

死锁
线程阻塞，等待永远不可能为真的条件成立。
死锁1：两根管道建立 		申请多个资源但已被其他线程持有	  	循环等待
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650275023307-7c9d267a-db0d-4545-9e71-c66fb39a3a40.png)
修改顺序
死锁2：持有锁的线程异常终止

- 在线程主动终止的任何分支上都需要解锁
- 如果线程可能被cancel，在加锁之后立刻pthread_cleanup_push

**pthread_mutex_lock 不是取消点 **

死锁3：一个线程对同一把锁加锁两次
```c
#include <43func.h>
int main(){
    //pthread_mutex_t mutex = PTHREAD_MUTEX_INITIALIZER;//静态初始化
    pthread_mutex_t mutex;
    pthread_mutex_init(&mutex,NULL);//如果第二个参数是NULL，表示以默认形式初始化锁
    pthread_mutex_lock(&mutex);//第一次加锁
    puts("lock one time!");
    pthread_mutex_lock(&mutex);//第二次加锁
    puts("lock two time!");
    pthread_mutex_destroy(&mutex);
}
```
解决死锁3的三种方法（上、中、下）

- 不写这种代码
- 非阻塞加锁 `pthread_mutex_trylock( )`
   - 未加锁，trylock会加锁，并返回 0
   - 已经加锁 ，trylock会立即返回 EBUSY
- 修改锁的属性 （ 不重要 ）

#include <pthread.h>
int pthread_mutex_trylock(pthread_mutex_t *mutex);
int pthread_mutex_unlock(pthread_mutex_t *mutex);
```c
#include <43func.h>
int main(){
    pthread_mutex_t mutex;
    pthread_mutex_init(&mutex,NULL);//如果第二个参数是NULL，表示以默认形式初始化锁
    int ret = pthread_mutex_trylock(&mutex);//第一次加锁
    THREAD_ERROR_CHECK(ret,"pthread_mutex_trylock 1");
    ret = pthread_mutex_trylock(&mutex);//第二次加锁
    THREAD_ERROR_CHECK(ret,"pthread_mutex_trylock 2");
    pthread_mutex_destroy(&mutex);
}
// pthread_mutex_trylock 2:Device or resource busy
```
trylock + while(1) 可以实现 自旋锁

```c
pthread_mutex_t mutex A,B;

// 第一个线程
while(1){
    int ret = pthread_mutex_trylock(&A);
    if (ret != 0){
        contiune;
    }
    // 说明已确认得到A,尝试得到B
    ret = pthread_mutex_trylock(&B);
    if (ret != 0){
        pthread_mutex_unlock(&A);
        printf(" 1 give up all, and wait for next!\n");
        contiune;
    }

// 第二个线程
while(1){
    int ret = pthread_mutex_trylock(&B);
    if (ret != 0){
        contiune;
    }
    // 说明已确认得到B,尝试得到A
    ret = pthread_mutex_trylock(&A);
    if (ret != 0){
        pthread_mutex_unlock(&B);
        printf(" 2 give up all, and wait for next!\n");
        contiune;
    }
```
trylock 可能会导致 **活锁 **

- **活锁解锁方法**
   - **// 让任意一个进程执行操作后 sleep( rand() ) **
   - **// 鸵鸟算法**

mutex的底层实现原理

- **mutex 	互斥睡眠锁 	// 加锁时，被阻塞会睡眠 **
- rwlock 	读写锁  // 多读少写
- spinlock	自旋锁  // 加锁时，被阻塞会 while(1) // 条件马上会就绪时较佳

mutex 使用 futex ( 智能 选择睡眠和自选行为）

mutex和二元信号量的区别
几乎无差别
用法差别： lock - P   unlock - V  但lock & unlock需要遵循“谁加锁谁解锁”

#### 使用锁火车票卖票
```c
#include <43func.h>
typedef struct shareRes_s{
    int trainTicket;
    pthread_mutex_t mutex;
} shareRes_t;
void *sellTicket1(void *arg){
    int cnt = 0;
    shareRes_t * pShareRes = (shareRes_t *)arg;
    while(1){
        pthread_mutex_lock(&pShareRes->mutex);
        if(pShareRes->trainTicket <= 0){
            //在所有离开循环的场景，记得解锁
            pthread_mutex_unlock(&pShareRes->mutex);
            break;
        }
        printf("before 1 sell ticket, num = %d\n", pShareRes->trainTicket);
        --pShareRes->trainTicket;
        ++cnt;
        printf("after 1 sell ticket, num = %d\n", pShareRes->trainTicket);
        pthread_mutex_unlock(&pShareRes->mutex);
        //sleep(1);
    }
    printf("sellticket1 = %d\n", cnt);
}
void *sellTicket2(void *arg){
    int cnt = 0;
    shareRes_t * pShareRes = (shareRes_t *)arg;
    while(1){
        pthread_mutex_lock(&pShareRes->mutex);
        if(pShareRes->trainTicket <= 0){
            //在所有离开循环的场景，记得解锁
            pthread_mutex_unlock(&pShareRes->mutex);
            break;
        }
        printf("before 2 sell ticket, num = %d\n", pShareRes->trainTicket);
        --pShareRes->trainTicket;
        ++cnt;
        printf("after 2 sell ticket, num = %d\n", pShareRes->trainTicket);
        pthread_mutex_unlock(&pShareRes->mutex);
        //sleep(1);
    }
    printf("sellticket2 = %d\n", cnt);
}
int main(){
    shareRes_t shareRes;
    shareRes.trainTicket = 20000000;
    pthread_mutex_init(&shareRes.mutex,NULL);
    pthread_t tid1,tid2;
    pthread_create(&tid1,NULL,sellTicket1,(void *)&shareRes);
    pthread_create(&tid2,NULL,sellTicket2,(void *)&shareRes);
    pthread_join(tid1,NULL);
    pthread_join(tid2,NULL);
    pthread_mutex_destroy(&shareRes.mutex);
}
```
while( ticket ){ } // 查看票的数量需要放入锁的保护
对共享资源的访问要处于锁的保护下
```c
while(1) {
    if(ticket<=0)
        unlock;
        break;
}
```
在所有离开循环的场景都需要解锁。

锁 是公平的 当数足够大
_拓展：优先级 反转 算法_

修改锁的属性（ 不重要 ）
int pthread_mutex_init(pthread_mutex_t *restrict mutex,
           const pthread_mutexattr_t *restrict attr);

-  const pthread_mutexattr_t *restrict attr // 锁的属性
   - 影响重复的锁的行为
   - 检错锁
   - 递归锁/可重入锁

$ man 3posix pthread_mutex_destory
```c
int pthread_mutexattr_init(pthread_mutexattr_t *attr);
int pthread_mutexattr_destroy(pthread_mutexattr_t *attr);
int pthread_mutexattr_settype(pthread_mutexattr_t *attr, int kind);
int pthread_mutexattr_gettype(const pthread_mutexattr_t *attr, int *kind);
$ man 3posix pthread_mutex_destory
```
$ man 3posix pthread_mutexattr_gettype
```c
PTHREAD_MUTEX_NORMAL	// 普通锁
PTHREAD_MUTEX_ERRORCHECK// 检错锁
PTHREAD_MUTEX_RECURSIVE	// 递归锁
PTHREAD_MUTEX_DEFAULT	// 默认锁
```

- 普通锁和默认一样
- 检错锁，
   - 对同一把锁加锁第二次加锁会出错
   - 加锁时，已被其他线程持有，则阻塞
- 递归锁（可重入锁），对同一把锁加锁第二次可以成功
   - 一个线程对同一把锁重复加锁，只是增加锁的引用计数
   - **其他线程**只有在**锁的引用计数为0**时，才能加锁，否则阻塞
   - 一个线程每次加锁，都需该线程**自己解锁**减少引用计数
```c
#include <43func.h>
void *func(void *pmutex){
    pthread_mutex_trylock((pthread_mutex_t *)pmutex);
}
int main(){
    pthread_mutex_t mutex;
    pthread_mutexattr_t mutexattr;
    pthread_mutexattr_init(&mutexattr);
    //pthread_mutexattr_settype(&mutexattr,PTHREAD_MUTEX_NORMAL);//普通锁和默认是一样的
    pthread_mutexattr_settype(&mutexattr,PTHREAD_MUTEX_ERRORCHECK);//检错锁，对同一把锁加锁，第二次加锁会报错
    //pthread_mutexattr_settype(&mutexattr,PTHREAD_MUTEX_RECURSIVE);//可重入锁，对同一把锁加锁
    pthread_mutex_init(&mutex,&mutexattr);
    int ret = pthread_mutex_lock(&mutex);//第一次加锁
    THREAD_ERROR_CHECK(ret,"pthread_mutex_lock 1");
    //ret = pthread_mutex_lock(&mutex);//第二次加锁
    //THREAD_ERROR_CHECK(ret,"pthread_mutex_lock 2");
    pthread_t tid;
    pthread_create(&tid,NULL,func,(void *)&mutex);
    pthread_join(tid,NULL);
    pthread_mutex_destroy(&mutex);
    pthread_mutexattr_destroy(&mutexattr);
}
```

同步
同步：事件的执行顺序是固定的
利用mutex 实现同步。
```c
// t1进程
A
flag = true;

// t2 进程
while(1){
    if(flag){
        break;
    }
    else
        continue;
}
B
```
完成后一个事件的线程，有极大可能，因为等待另一个线程完成前一个事件，CPU空转一段时间

### 条件变量
实现事件的同步  	无竞争的事件会合场景
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650369189522-0a7fc5a3-64b6-4905-93a3-3e6842a18628.png)

1. 设计一个条件，这个条件决定了本线程是否需要等待
1. 如果不满足条件，调用wait会使本线程陷入等待
1. 此时，其他线程会运行，直到条件满足，通知(signal)阻塞的线程恢复就绪

条件变量的接口
条件变量必须配合**锁**一起使用

1. 只有加锁的状态下，才能够使用wait
1. 从wait中唤醒的话，说明前面的事件已经完成
1. 解锁
```c
// static init
pthread_cond_t cond = PTHREAD_COND_INITIALIZER;
// dynamic init									NULL
int pthread_cond_init(pthread_cond_t *cond, pthread_condattr_t *cond_attr);
// 唤醒 等待线程
int pthread_cond_signal(pthread_cond_t *cond);

int pthread_cond_broadcast(pthread_cond_t *cond);
// 线程等待
int pthread_cond_wait(pthread_cond_t *cond, pthread_mutex_t *mutex);

int pthread_cond_timedwait(pthread_cond_t *cond, pthread_mutex_t *mutex, const
struct timespec *abstime);

int pthread_cond_destroy(pthread_cond_t *cond);
```

使用条件变量的一般流程

- 清楚事件的发生顺序    先-signal   后-wait

```c
#include <43func.h>
typedef struct shareRes_s
{
    int flag;
    pthread_cond_t cond;
    pthread_mutex_t mutex;
} shareRes_t;
void *threadFunc(void *arg)
{
    shareRes_t *pShareRes = (shareRes_t *)arg;

    //先加锁
    pthread_mutex_lock(&pShareRes->mutex);
    //只有在加锁的状态下，才能够使用wait
    if (pShareRes->flag == 0)
    {
        pthread_cond_wait(&pShareRes->cond, &pShareRes->mutex);
    }
    //从wait中唤醒的话，说明前面的事件已经完成了
    puts("Last");
    puts("world");
    //记得解锁
    pthread_mutex_unlock(&pShareRes->mutex);
    puts("child end");

    pthread_exit(NULL);
}
int main()
{
    //初始化锁和条件变量
    shareRes_t shareRes;
    pthread_mutex_init(&shareRes.mutex, NULL);
    pthread_cond_init(&shareRes.cond, NULL);
    shareRes.flag = 0;
    //创建一个子线程
    pthread_t tid;
    pthread_create(&tid, NULL, threadFunc, (void *)&shareRes);

    sleep(1);
    pthread_mutex_lock(&shareRes.mutex);
    //先执行一个事件，然后唤醒等待在条件变量上的某个线程
    puts("\nHello");
    shareRes.flag = 1;
    pthread_cond_signal(&shareRes.cond);
    puts("First\n\n");
    sleep(2);
    puts("Then");
    pthread_mutex_unlock(&shareRes.mutex);
    sleep(2);
    puts("End");

    //收尾工作 等待子线程的终止，销毁锁和条件变量
    pthread_join(tid, NULL);
    pthread_cond_destroy(&shareRes.cond);
    pthread_mutex_destroy(&shareRes.mutex);
}
//
// Hello
// First
//
//
// Then
// End
// Last
// world
// child end

```
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650351985698-e226f3e3-fd8f-417d-b9d3-51bbff35ab97.png)
加锁 是为了 保护 “检查flag状态，并wait ”是原子操作
“检查flag状态，并signal”也需要加锁

*******pthread_cond_wait的内部实现***** **
前一半

1. 判断是否**已先加锁 **，若已加锁，则继续；没有加锁，抛出异常
1. 把自己加入条件变量的唤醒等待队列
1. **解锁**并陷入等待 （原子操作）（ 自己维护一把锁，加锁解锁 ）

后一半（收到**signal**之后）

4. 本线程就绪
4. **加锁 （若锁被其他进程持有，则阻塞）**
4. 将**持有锁**之后，继续运行

在signal之时，应有线程已调用wait而阻塞，否则这个signal就丢失了

使用条件变量的惯用法：

1. 希望wait的线程先执行
   1. 先加锁（为了保护flag）
   1. 判断状态是否满足
   1. 调用cond_wait
   1. 解锁
2. 先执行的signal线程
   1. 先加锁（可以不加，一般加）
   1. signal线程要执行的事件
   1. 修改flag并用signal通知
   1. 事件完毕后再解锁
3. 再执行wait的下半部
   - 执行后续事件，然后解锁。

#### 卖火车票
```c
#include <43func.h>
typedef struct shareRes_s{
    int trainTicket;
    int flag;
    pthread_mutex_t mutex;
    pthread_cond_t cond;
} shareRes_t;
void * sellTicket1(void *arg){
    sleep(1);//确保addTicket能够持有锁并wait
    shareRes_t *pShareRes = (shareRes_t *)arg;
    while(1){
        sleep(1);
        pthread_mutex_lock(&pShareRes->mutex);
        if(pShareRes->trainTicket > 0 ){
            printf("Before 1 sell, ticket = %d\n", pShareRes->trainTicket);
            --pShareRes->trainTicket;
            printf("After 1 sell, ticket = %d\n", pShareRes->trainTicket);
            if(pShareRes->trainTicket == 0 && pShareRes->flag == 0){
                //如果卖完票之后，没有票了，而且从来没有放过票
                pthread_cond_signal(&pShareRes->cond);
                pShareRes->flag = 1;
            }
            else if(pShareRes->trainTicket == 0 && pShareRes->flag != 0){
                //如果卖完票而且已经放过票
                pthread_mutex_unlock(&pShareRes->mutex);
                break;
            }
        }
        else//{
            //if( pShareRes->flag == 0){
                // 唤醒卖票与加票时，卖票先抢到，走此分支
                //pthread_mutex_unlock(&pShareRes->mutex);
                //continue;
            //}
            //else{
            // 仅一张票，抢到锁的线程走上面的if，另一个没抢到的线程走此else分支
            pthread_mutex_unlock(&pShareRes->mutex);
            break;
        }
        pthread_mutex_unlock(&pShareRes->mutex);
    }
    pthread_exit(NULL);
}
void * sellTicket2(void *arg){
    sleep(1);//确保addTicket能够持有锁并wait
    shareRes_t *pShareRes = (shareRes_t *)arg;
    while(1){
        sleep(1);
        pthread_mutex_lock(&pShareRes->mutex);
        if(pShareRes->trainTicket > 0){
            printf("Before 2 sell, ticket = %d\n", pShareRes->trainTicket);
            --pShareRes->trainTicket;
            printf("After 2 sell, ticket = %d\n", pShareRes->trainTicket);
            if(pShareRes->trainTicket == 0 && pShareRes->flag == 0){
                //如果卖完票之后，没有票了，而且从来没有放过票
                pthread_cond_signal(&pShareRes->cond);
                pShareRes->flag = 1;
            }
            else if(pShareRes->trainTicket == 0 && pShareRes->flag != 0){
                //如果卖完票而且已经放过票
                pthread_mutex_unlock(&pShareRes->mutex);
                break;
            }
        }
        else{
            // 仅一张票，抢到锁的线程走上面的if，另一个没抢到的线程走此else分支
            pthread_mutex_unlock(&pShareRes->mutex);
            break;
        }
        pthread_mutex_unlock(&pShareRes->mutex);
    }
    pthread_exit(NULL);
}
void * addTicket(void *arg){
    shareRes_t *pShareRes = (shareRes_t *)arg;
    pthread_mutex_lock(&pShareRes->mutex);
    if(pShareRes->flag != 1){
        pthread_cond_wait(&pShareRes->cond, &pShareRes->mutex);
    }
    printf("add ticket!\n");
    pShareRes->trainTicket = 10;
    pthread_mutex_unlock(&pShareRes->mutex);
    pthread_exit(NULL);
}
int main(){
    shareRes_t shareRes;
    shareRes.trainTicket = 20;//初始票的数量是20
    shareRes.flag = 0;//最开始不需要放票
    pthread_mutex_init(&shareRes.mutex,NULL);
    pthread_cond_init(&shareRes.cond,NULL);
    pthread_t tid1,tid2,tid3;
    pthread_create(&tid1,NULL,sellTicket1,(void *)&shareRes);
    pthread_create(&tid2,NULL,sellTicket2,(void *)&shareRes);
    pthread_create(&tid3,NULL,addTicket,(void *)&shareRes);
    pthread_join(tid1,NULL);
    pthread_join(tid2,NULL);
    pthread_join(tid3,NULL);
    pthread_mutex_destroy(&shareRes.mutex);
    pthread_cond_destroy(&shareRes.cond);
}
```
小bug：判断一下会不会从来没有放过票，卖票1、卖票2就中止了。

生产者和消费者
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650357573517-a61ada58-e6dd-46ad-82cb-73ac81b6eb0d.png)
#### 顺序队列 实现 producer and consumer
```c
#include <43func.h>
#define NUM 10
typedef struct queue_s{
    int elem[NUM];
    int size;
    int front;//front表示队首的下标
    int rear;//rear表示下一个要入队的元素的下标
} queue_t;
typedef struct shareRes_s{
    queue_t queue;
    pthread_mutex_t mutex;
    pthread_cond_t cond;
} shareRes_t;
int cnt = 0;
void * producer(void *arg){
    shareRes_t * pShareRes = (shareRes_t *)arg;
    while(1){
        pthread_mutex_lock(&pShareRes->mutex);
        if(pShareRes->queue.size == 10){
            pthread_cond_wait(&pShareRes->cond,&pShareRes->mutex);
        }
        pShareRes->queue.elem[pShareRes->queue.rear] = cnt++;
        pShareRes->queue.rear = (pShareRes->queue.rear + 1) %NUM;
        ++pShareRes->queue.size;
        printf("producer size = %d, front = %d, rear = %d\n",
            pShareRes->queue.size,
            pShareRes->queue.elem[pShareRes->queue.front],
            pShareRes->queue.elem[(pShareRes->queue.rear -1 + NUM)%NUM]);
        if(pShareRes->queue.size == 1){
            pthread_cond_signal(&pShareRes->cond);
        }
        pthread_mutex_unlock(&pShareRes->mutex);
        usleep(500000);
    }
}
void * consumer(void *arg){
    shareRes_t * pShareRes = (shareRes_t *)arg;
    while(1){
        pthread_mutex_lock(&pShareRes->mutex);
        if(pShareRes->queue.size == 0){
            pthread_cond_wait(&pShareRes->cond,&pShareRes->mutex);
        }
        pShareRes->queue.front = (pShareRes->queue.front + 1) %NUM;
        --pShareRes->queue.size;
        printf("consumer size = %d, front = %d, rear = %d\n",
            pShareRes->queue.size,
            pShareRes->queue.elem[pShareRes->queue.front],
            pShareRes->queue.elem[(pShareRes->queue.rear -1 + NUM)%NUM]);
        if(pShareRes->queue.size == 9){
            pthread_cond_signal(&pShareRes->cond);
        }
        pthread_mutex_unlock(&pShareRes->mutex);
        sleep(1);
    }
}
int main(){
    shareRes_t shareRes;
    bzero(&shareRes.queue,sizeof(shareRes.queue));
    pthread_mutex_init(&shareRes.mutex,NULL);
    pthread_cond_init(&shareRes.cond,NULL);
    pthread_t tid1,tid2;
    pthread_create(&tid1,NULL,producer,(void *)&shareRes);
    pthread_create(&tid2,NULL,consumer,(void *)&shareRes);
    pthread_join(tid1,NULL);
    pthread_join(tid2,NULL);
    pthread_mutex_destroy(&shareRes.mutex);
    pthread_cond_destroy(&shareRes.cond);
}
```
#### `pthread_cond_timedwait`
pthread_cond_timedwait — wait on a condition  // 绝对时间
#include <pthread.h>
int pthread_cond_timedwait(pthread_cond_t *restrict cond,
           pthread_mutex_t *restrict mutex,
           const struct timespec *restrict abstime);
```c
#include <43func.h>
int main(){
    pthread_mutex_t mutex;
    pthread_mutex_init(&mutex,NULL);
    pthread_cond_t cond;
    pthread_cond_init(&cond,NULL);man
    pthread_mutex_lock(&mutex);
    ///pthread_cond_wait(&cond,&mutex);
    struct timespec abstime;
    time_t now = time(NULL);
    puts(ctime(&now));
    abstime.tv_sec = now+3;
    abstime.tv_nsec = 0;
    pthread_cond_timedwait(&cond,&mutex,&abstime);
    now = time(NULL);
    puts(ctime(&now));
    pthread_mutex_destroy(&mutex);
    pthread_cond_destroy(&cond);
}
```

#### thread_cond_broadcast 广播
signal 会唤醒对应条件变量的等待队列里的队首
broadcast会唤醒对应条件变量的等待队列中所有的线程
唤醒所有线程，防止所有线程执行，引起**期望外的结果**，可以使用**while( )** 避免。
```c
#include <43func.h>
typedef struct shareRes_s{
    int cnt;
    pthread_cond_t cond;
    pthread_mutex_t mutex;
} shareRes_t;
void * threadFunc(void *arg){
    shareRes_t * pShareRes = (shareRes_t *)arg;
    pthread_mutex_lock(&pShareRes->mutex);

    //if(pShareRes->cnt == 0){
    while(pShareRes->cnt == 0){
        pthread_cond_wait(&pShareRes->cond,&pShareRes->mutex);
    }
    printf("cnt = %d\n", pShareRes->cnt);
    pShareRes->cnt = pShareRes->cnt - 1;
    printf("cnt = %d\n", pShareRes->cnt);
    pthread_mutex_unlock(&pShareRes->mutex);

    pthread_exit(NULL);
}
int main(){
    //初始化锁和条件变量
    shareRes_t shareRes;
    pthread_mutex_init(&shareRes.mutex,NULL);
    pthread_cond_init(&shareRes.cond,NULL);
    shareRes.cnt = 0;
    //创建一个子线程
    pthread_t tid1;
    pthread_create(&tid1,NULL, threadFunc, (void *)&shareRes);
    pthread_t tid2;
    pthread_create(&tid2,NULL, threadFunc, (void *)&shareRes);

    sleep(1);
    pthread_mutex_lock(&shareRes.mutex);
    //先执行一个事件，然后唤醒等待在条件变量上的某个线程
    shareRes.cnt = 1;
    //pthread_cond_signal(&shareRes.cond);
    pthread_cond_broadcast(&shareRes.cond);
    pthread_mutex_unlock(&shareRes.mutex);

    //收尾工作 等待子线程的终止，销毁锁和条件变量
    pthread_join(tid1,NULL);
    pthread_join(tid2,NULL);
    pthread_cond_destroy(&shareRes.cond);
    pthread_mutex_destroy(&shareRes.mutex);
}
```
broadcast + **while( ) 	// while( )**可以**避免**内核或者其他人**虚假唤醒**

### 线程安全 ( 对 库函数 来说）
有一些库函数在多线程的情况下运行可能不正确
多是因为库函数使用了堆、数据段(全局变量、static)共享区域的内存
char *ctime(const time_t *time)

- malloc( ) 		安全，但效率较低
- tcmalloc( ) 	效率较高
```c
#include <43func.h>
void *threadFunc(void *arg){
    time_t now = time(NULL);
    char buf[1024] = {0};
    //char *p = ctime(&now);
    char *p = ctime_r(&now,buf);
    printf("child p = %s\n", p);
    sleep(5);
    printf("child p = %s\n", p);
}
int main(){
    pthread_t tid;
    pthread_create(&tid,NULL,threadFunc,NULL);
    sleep(2);
    time_t mainNow = time(NULL);
    char buf[1024] = {0};
    //char *p = ctime(&mainNow);
    char *p = ctime_r(&mainNow,buf);
    printf("main p = %s\n", p);
    pthread_join(tid,NULL);
}
```
![image.png](https://cdn.nlark.com/yuque/0/2022/png/916648/1650362008316-2fb13007-34f0-4132-85f2-1c9620373a6b.png)

#### 线程安全版本：把内存分配交给主调函数
char *ctime_r(const time_t *timep, char *buf)

- char *buf   	主调函数使用

#### 可重入和不可重入
一个函数在调用过程中，有机会再次调用自己

- 同步   ——    递归
- 异步	地调用自己 - 重入
   - 信号处理函数
   - 多线程

如果一个函数在重入后的结果和不重入的结果一样 - 可重入函数

可重入的函数特点：

- 不加锁，不访问共享区域
- 不修改代码段
- 不调用不可重入函数
